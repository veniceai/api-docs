---
title: '텍스트 파서'
openapi: 'POST /augment/text-parser'
"og:title": "텍스트 파서 | Venice API Docs"
"og:description": "PDF, DOCX, XLSX 및 일반 텍스트 파일에서 텍스트를 추출하는 Venice의 텍스트 파서 API에 대한 문서입니다."
---

<Warning>이는 실험적 API입니다. 요청 및 응답 형식은 사전 공지 없이 변경될 수 있습니다.</Warning>

`file` 필드를 사용해 multipart/form-data로 문서 파일을 업로드하세요. 지원 형식은 **PDF**, **DOCX**, **XLSX** 및 **일반 텍스트** 파일(최대 25MB)입니다.

추출된 텍스트와 토큰 수가 포함된 구조화된 출력을 원하면 `response_format`을 `json`(기본값)으로 설정하고, 원시 추출 텍스트를 원하면 `text`로 설정하세요.

**프라이버시:** 텍스트 파싱은 zero data retention이 적용된 Venice 인프라의 메모리 내에서만 실행됩니다. 문서는 처리 후 즉시 폐기되며, 어떠한 콘텐츠도 저장되거나 기록되지 않습니다.

**가격:** 요청당 $0.01.

### 예제 (cURL)

```bash
curl -X POST https://api.venice.ai/api/v1/augment/text-parser \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -F "file=@document.pdf" \
  -F "response_format=json"
```

-------


