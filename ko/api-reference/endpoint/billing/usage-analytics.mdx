---
title: 'Billing Usage Analytics (Beta)'
openapi: 'GET /billing/usage-analytics'
"og:title": "결제 사용량 분석 | Venice API Docs"
"og:description": "날짜, 모델 및 API 키별 분석이 포함된 집계 사용량 분석을 가져옵니다."
---

<Warning>
이는 베타 엔드포인트이며 불안정하거나 사전 공지 없이 변경될 수 있습니다.
</Warning>

인증된 사용자에 대해 날짜, 모델 및 API 키별로 세분화된 집계 사용량 분석을 조회합니다. 이 엔드포인트는 대시보드를 구축하고 소비를 모니터링하기 위해 API 사용 데이터의 요약 보기를 제공합니다. 데이터는 10분 동안 캐시됩니다.

## 쿼리 파라미터

다음 중 하나를 사용해 분석 기간을 지정할 수 있습니다.
- **lookback**: "7d"(7일), "30d"(30일) 같은 상대 기간으로 최대 "90d"(90일)까지.
- **startDate** 및 **endDate**: `YYYY-MM-DD` 형식의 사용자 지정 날짜 범위. 하나라도 제공되면 둘 다 필수입니다.

파라미터를 지정하지 않으면 기본 lookback 기간은 7일입니다.

## 응답 필드

### lookback
쿼리에 사용된 lookback 기간. "Nd" 형식(예: "7d") 또는 "startDate:endDate" 형식.

### byDate
요청된 기간의 일별 사용량 합계.

- **date**: `YYYY-MM-DD` 형식의 날짜
- **USD**: 해당 일의 USD 총 사용량
- **DIEM**: 해당 일의 DIEM 총 사용량

### byModel
모델별 사용량 분석, 총 지출 기준 정렬(높은 순).

- **modelName**: 모델의 표시 이름(예: "GLM 5")
- **unitType**: 소비된 단위 유형 (tokens, images, chars, minutes, seconds)
- **modelType**: 모델 유형 (LLM, IMAGE, TTS, ASR, VIDEO) 또는 null
- **totalUsd**: 이 모델에 지출된 총 USD
- **totalDiem**: 이 모델에 지출된 총 DIEM
- **totalUnits**: 이 모델에 소비된 총 단위
- **breakdown**: 유형별 사용량 분석 배열 (여러 유형이 있을 때만 표시). 각 항목은 다음을 포함합니다.
  - **type**: 토큰 유형(예: "Input", "Output", "Cache Read", "Cache Write")
  - **usd**: 이 분석에 대한 USD 금액
  - **diem**: 이 분석에 대한 DIEM 금액
  - **units**: 이 분석에 대한 단위 수

### byModelDaily
상위 8개 모델의 일별 차트 데이터. 각 항목에는 "date"(타임스탬프)와 DIEM 사용량 값을 가진 모델 이름 키가 포함됩니다.

### topModels
차트 범례를 위한 사용량 기준 상위 8개 모델 이름 배열.

### byKey
API 키별 사용량 분석, 총 지출 기준 정렬(높은 순).

- **apiKeyId**: API 키 ID, 또는 웹 앱에서의 사용량인 경우 null
- **description**: API 키 설명 또는 "Web App"
- **totalUsd**: 이 키를 통해 지출된 총 USD
- **totalDiem**: 이 키를 통해 지출된 총 DIEM
- **totalUnits**: 이 키를 통해 소비된 총 단위

### byKeyDaily
상위 8개 API 키의 일별 차트 데이터. 각 항목에는 "date"(타임스탬프)와 DIEM 사용량 값을 가진 키 설명이 포함됩니다.

### topKeyNames
차트 범례를 위한 사용량 기준 상위 8개 API 키 설명 배열.

## 예제 사용

```bash
# 지난 7일간의 사용량 분석 가져오기 (기본값)
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics" \
  -H "Authorization: Bearer YOUR_API_KEY"

# 지난 30일간의 사용량 분석 가져오기
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics?lookback=30d" \
  -H "Authorization: Bearer YOUR_API_KEY"

# 특정 날짜 범위의 사용량 분석 가져오기
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics?startDate=2024-01-01&endDate=2024-01-31" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

