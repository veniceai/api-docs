---
title: '이미지 멀티 편집'
openapi: 'POST /image/multi-edit'
"og:title": "이미지 멀티 편집 | Venice API Docs"
"og:description": "최대 3개의 레이어드 입력(베이스 이미지 + 마스크/오버레이)을 사용해 이미지를 편집합니다."
---

<Info>
**가격:** 멀티 편집 가격은 모델별로 다릅니다. 현재 편집당 가격은 [가격 개요](/ko/overview/pricing)
를 참고하세요.
</Info>

## 품질 등급

일부 멀티 편집 모델은 시각적 품질과 비용을 절충하는 선택적 `quality` 파라미터(`low`, `medium`, `high`)를 받습니다. 현재 `gpt-image-2-edit`에서 지원되며, 다른 모델은 해당 파라미터를 무시합니다.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

`quality`를 생략하면 모델은 기본 등급(`gpt-image-2-edit`의 경우 `high`)을 사용합니다. 등급별 가격(1K/2K/4K × low/medium/high)은 [Models 엔드포인트](/ko/api-reference/endpoint/models/list)의 `model_spec.pricing.quality` 아래에 있으며 [가격 개요](/ko/overview/pricing)에도 나열되어 있습니다.

<Info>
품질 기반 가격 책정은 모든 API 및 SDK 호출자에 대해 기본값입니다 — 별도의 opt-in 헤더가 필요하지 않습니다. `quality`를 생략한 요청은 모델의 기본 등급(`gpt-image-2-edit`의 경우 `high`)으로 청구됩니다.
</Info>
