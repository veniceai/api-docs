---
title: 'Create Decision (SystemOne alias)'
openapi: 'POST /systemone'
'og:title': 'Decisions (SystemOne alias) | Venice API Docs'
'og:description': 'TypeSafe SDK-compatible alias of POST /decisions.'
---

<Warning>
**Beta**: This endpoint is currently in beta and may be unstable. Request/response schemas and behavior may change without notice.
</Warning>

Alias of [Create Decision](/api-reference/endpoint/decisions/create) mirroring the upstream TypeSafe path, so the official TypeSafe SDKs work with only a base URL and API key swap:

```bash
export TYPESAFE_BASE_URL=https://api.venice.ai/api
export TYPESAFE_API_KEY=$VENICE_API_KEY
```
