---
title: 'Bild-Multi-Edit'
openapi: 'POST /image/multi-edit'
"og:title": "Bild-Multi-Edit | Venice API Docs"
"og:description": "Bilder mit bis zu drei geschichteten Eingaben bearbeiten (Basisbild plus Masken/Overlays)."
---

<Info>
**Preis:** Die Multi-Edit-Preise variieren je nach Modell. Aktuelle Preise pro Bearbeitung finden Sie in der [Preisübersicht](/de/overview/pricing).
</Info>

## Qualitätsstufen

Einige Multi-Edit-Modelle akzeptieren einen optionalen `quality`-Parameter (`low`, `medium`, `high`), der visuelle Qualität gegen Kosten abwägt. Wird derzeit von `gpt-image-2-edit` unterstützt; andere Modelle ignorieren den Parameter.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

Wenn Sie `quality` weglassen, verwendet das Modell seine Standardstufe (`high` für `gpt-image-2-edit`). Preise pro Stufe (1K/2K/4K × low/medium/high) finden Sie unter `model_spec.pricing.quality` im [Models-Endpoint](/de/api-reference/endpoint/models/list) sowie in der [Preisübersicht](/de/overview/pricing).

<Info>
Qualitätsabhängige Preise sind der Standard für alle API- und SDK-Aufrufer — kein Opt-in-Header erforderlich. Anfragen ohne `quality` werden zur Standardstufe des Modells abgerechnet (`high` für `gpt-image-2-edit`).
</Info>
