---
title: "Crypto RPC"
description: "Entdecken Sie unterstützte Blockchain-Netzwerke und senden Sie JSON-RPC-Anfragen über Venice mit API-Schlüssel- oder x402-Wallet-Authentifizierung."
"og:title": "Crypto RPC | Venice API Docs"
"og:description": "Greifen Sie über Venice auf unterstützte Blockchain-Netzwerke zu, ohne einen separaten RPC-Anbieter verwalten zu müssen."
---

Venice Crypto RPC ist ein HTTP-Proxy zum Lesen des Blockchain-Zustands und zum Weiterleiten signierter Transaktionen. Es verwendet dieselbe Venice-API-Schlüssel- oder x402-Wallet-Authentifizierung wie andere kostenpflichtige Venice-Endpunkte, sodass Ihre Anwendung kein separates Konto bei einem RPC-Anbieter benötigt.

Venice hält niemals Ihre privaten Schlüssel. Signieren Sie Transaktionen in Ihrer Anwendung und übermitteln Sie die signierte Payload mit `eth_sendRawTransaction`.

## Unterstützte Netzwerke ermitteln

Rufen Sie die aktuelle Liste der Netzwerk-Slugs ab:

```bash
curl https://api.venice.ai/api/v1/crypto/rpc/networks
```

Der Discovery-Endpunkt ist öffentlich und erfordert keine Authentifizierung. Seine Antwort ist maßgeblich; verwenden Sie einen der zurückgegebenen Slugs als `{network}` im RPC-Endpunkt.

## Eine RPC-Anfrage senden

Senden Sie eine JSON-RPC 2.0-Anfrage an das ausgewählte Netzwerk:

```bash
curl https://api.venice.ai/api/v1/crypto/rpc/ethereum-mainnet \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_blockNumber",
    "params": [],
    "id": 1
  }'
```

Der Proxy unterstützt einzelne Anfragen und Batches von bis zu 100 Anfragen. Unterstützte Methoden, Preisstufen und Verfügbarkeit variieren; konsultieren Sie die [Crypto RPC API-Referenz](/api-reference/endpoint/crypto/rpc), bevor Sie sich auf eine Methode verlassen.

## Lese- und Schreib-Workflows

### Chain-Zustand lesen

Verwenden Sie Methoden wie `eth_blockNumber`, `eth_getBalance`, `eth_call` und `eth_getLogs`, um öffentliche Chain-Daten abzufragen. Bündeln Sie verwandte Aufrufe, wenn sie dasselbe Netzwerk betreffen.

### Eine Transaktion weiterleiten

<Steps>
  <Step title="Transaktion erstellen">
    Lesen Sie die Account-Nonce, Gebührendaten und die Gas-Schätzung aus dem Zielnetzwerk.
  </Step>
  <Step title="Lokal signieren">
    Signieren Sie mit der Wallet-Bibliothek Ihrer Anwendung. Senden Sie niemals einen privaten Schlüssel oder eine Seed-Phrase an Venice.
  </Step>
  <Step title="Signierte Payload übermitteln">
    Rufen Sie `eth_sendRawTransaction` auf und setzen Sie einen `Idempotency-Key`-Header, damit Wiederholungen keine zweite abrechenbare Weiterleitung erzeugen.
  </Step>
  <Step title="Beleg prüfen">
    Rufen Sie `eth_getTransactionReceipt` wiederholt ab, bis die Transaktion enthalten ist, und prüfen Sie dann ihren Status.
  </Step>
</Steps>

## Sichere Wiederholungen

Setzen Sie einen Idempotenzschlüssel bei Anfragen, die wiederholt werden könnten, insbesondere bei Transaktionsweiterleitungen:

```bash
-H "Idempotency-Key: transaction-123"
```

Venice speichert die Antwort 24 Stunden lang zwischen. Die Wiederverwendung des Schlüssels mit demselben Anfrage-Body gibt die zwischengespeicherte Antwort ohne erneute Berechnung zurück; die Wiederverwendung mit einem anderen Body gibt einen Fehler zurück.

## Wichtige Einschränkungen

- Der Proxy ist ausschließlich HTTP-basiert und unterstützt keine WebSocket-Abonnements.
- Zustandsbehaftete Filtermethoden werden nicht unterstützt; verwenden Sie zustandslose Methoden wie `eth_getLogs`.
- Venice hält keine Schlüssel und stellt keine Signiermethoden bereit.
- Methoden außerhalb der Zulassungsliste geben einen Fehler zurück.
- Prüfen Sie `X-Venice-RPC-Credits` und `X-Venice-RPC-Cost-USD`, um die Anfragekosten zu verfolgen.

## Weiterführende Ressourcen

- [Supported Networks API](/api-reference/endpoint/crypto/networks)
- [Crypto RPC API-Referenz](/api-reference/endpoint/crypto/rpc)
- [Crypto RPC für Agenten](/guides/integrations/crypto-rpc-agents)
- [x402 Wallet-Authentifizierung](/guides/integrations/x402-venice-api)
