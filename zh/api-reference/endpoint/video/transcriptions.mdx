---
title: '视频转录 API (Beta)'
openapi: 'POST /video/transcriptions'
"og:title": "视频转录 | Venice API 文档"
"og:description": "介绍 Venice 视频转录 API 的文档，用于从视频音频中提取文本。"
---

在 `url` 中发送一个可公开访问的视频 URL。可选地将 `response_format` 设置为 `json`（默认）或 `text`，具体取决于你希望得到结构化输出还是纯文本转录。

-------


