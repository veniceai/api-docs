---
title: '文本解析器'
openapi: 'POST /augment/text-parser'
"og:title": "文本解析器 | Venice API 文档"
"og:description": "介绍 Venice 文本解析器 API 的文档，可从 PDF、DOCX、XLSX 和纯文本文件中提取文本。"
---

<Warning>这是一个实验性 API。请求和响应格式可能在不另行通知的情况下发生变更。</Warning>

通过 multipart/form-data 使用 `file` 字段上传文档文件。支持的格式包括 **PDF**、**DOCX**、**XLSX** 和**纯文本**文件（最大 25MB）。

将 `response_format` 设置为 `json`（默认）以获取包含已提取文本和 token 数的结构化输出，或设置为 `text` 以获取原始提取文本。

**隐私：** 文本解析完全在 Venice 基础设施的内存中运行，零数据留存。你的文档在处理后会被立即丢弃，不会存储或记录任何内容。

**定价：** 每次请求 0.01 美元。

### 示例（cURL）

```bash
curl -X POST https://api.venice.ai/api/v1/augment/text-parser \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -F "file=@document.pdf" \
  -F "response_format=json"
```

-------


