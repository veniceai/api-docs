---
title: '生成图像'
openapi: 'POST /image/generate'
"og:title": "Image Generate | Venice API Docs"
"og:description": "Documentation covering Venice's image generation API."
---

## 尺寸选项

图像模型使用模型专属的尺寸参数：

- 基于像素的模型接受 `width` 和 `height`，例如 `venice-sd35` 和 `qwen-image`。
- 基于宽高比的模型接受 `aspect_ratio`，例如 `qwen-image-2`。
- 基于分辨率等级的模型同时接受 `aspect_ratio` 和 `resolution`。

例如，`gpt-image-2`、`nano-banana-2` 和 `nano-banana-pro` 支持 `resolution` 取值 `1K`、`2K` 和 `4K`：

```json
{
  "model": "gpt-image-2",
  "prompt": "a cinematic wide shot of Venice at sunset",
  "aspect_ratio": "16:9",
  "resolution": "4K",
  "format": "png"
}
```

相同的分辨率分级尺寸模式也适用于 Nano Banana 模型：

```json
{
  "model": "nano-banana-pro",
  "prompt": "a serene canal in venice at sunset",
  "aspect_ratio": "16:9",
  "resolution": "2K"
}
```

对于接受宽高比但不公开分辨率等级的模型，请发送 `aspect_ratio` 而不带 `resolution`：

```json
{
  "model": "qwen-image-2",
  "prompt": "a serene canal in venice at sunset",
  "aspect_ratio": "16:9"
}
```

对于使用直接像素尺寸的模型，请改为发送 `width` 和 `height`：

```json
{
  "model": "venice-sd35",
  "prompt": "a serene canal in venice at sunset",
  "width": 1024,
  "height": 1024
}
```

在跨模型混用尺寸字段之前，请查看[图像模型](/models/image)或 [Models 端点](/api-reference/endpoint/models/list)了解每个模型支持的尺寸选项。

## 质量等级

部分模型接受可选的 `quality` 参数（`low`、`medium`、`high`），用于在视觉保真度和成本之间取得平衡。当前由 `gpt-image-2` 支持；其他模型会忽略该参数。

```json
{
  "model": "gpt-image-2",
  "prompt": "a cinematic wide shot of Venice at sunset",
  "aspect_ratio": "16:9",
  "resolution": "2K",
  "quality": "medium"
}
```

当您省略 `quality` 时，模型将使用其默认等级（`gpt-image-2` 为 `high`）。价格取决于 `resolution` 与 `quality` 的组合 —— `gpt-image-2` 的各等级价格（1K/2K/4K × low/medium/high）在 [Models 端点](/api-reference/endpoint/models/list)的 `model_spec.pricing.quality` 下公开，也列于[定价概览](/overview/pricing)中。

<Info>
质量感知定价是所有 API 和 SDK 调用方的默认行为 —— 无需显式启用的请求头。省略 `quality` 的请求按模型的默认等级（`gpt-image-2` 为 `high`）计费。
</Info>

## Postman 集合
如需更多示例，请参阅此 [Postman 集合](https://www.postman.com/veniceai/workspace/venice-ai-workspace/folder/38652128-0adc004d-2edf-4b88-a3bb-0f868c791c9c?action=share&source=copy-link&creator=38652128&ctx=documentation)。

-------

