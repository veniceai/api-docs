---
title: '编辑（即 Inpaint）'
openapi: 'POST /image/edit'
"og:title": "Image Edit | Venice API Docs"
"og:description": "Documentation covering Venice's image edit (aka inpaint) API."
---

<Warning>
这是一个实验性端点，可能会发生变化。
</Warning>

<Info>
**定价：** 图像编辑价格因模型而异。默认模型（`qwen-edit`）为**每次编辑 $0.04**。所有可用的编辑模型及其定价请参阅 [Models 端点](/api-reference/endpoint/models/list)。
</Info>

## 质量等级

部分编辑模型接受可选的 `quality` 参数（`low`、`medium`、`high`），用于在视觉保真度和成本之间取得平衡。当前由 `gpt-image-2-edit` 支持；其他模型会忽略该参数。

```json
{
  "model": "gpt-image-2-edit",
  "image": "iVBORw0KGgo...",
  "prompt": "change the sky to a sunrise",
  "resolution": "2K",
  "quality": "medium"
}
```

当您省略 `quality` 时，模型将使用其默认等级（`gpt-image-2-edit` 为 `high`）。各等级价格（1K/2K/4K × low/medium/high）位于 [Models 端点](/api-reference/endpoint/models/list)中 `model_spec.pricing.quality` 下，也在[定价概览](/overview/pricing)中列出。

<Info>
质量感知定价是所有 API 和 SDK 调用方的默认行为 —— 无需显式启用的请求头。省略 `quality` 的请求按模型的默认等级（`gpt-image-2-edit` 为 `high`）计费。
</Info>

## Postman 集合
如需更多示例，请参阅此 [Postman 集合](https://www.postman.com/veniceai/workspace/venice-ai-workspace/folder/38652128-2d156cd6-a9bc-4586-8a8b-98e4b5c4435d?action=share&source=copy-link&creator=38652128&ctx=documentation)。

-------

<Warning>
默认模型（`qwen-edit`）会拒绝任何尝试生成露骨色情图像、性化未成年人或描绘真实暴力的请求。其他模型可能有不同的内容政策。
</Warning>

