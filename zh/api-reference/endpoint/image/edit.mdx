---
title: '编辑（也称 Inpaint）'
openapi: 'POST /image/edit'
"og:title": "图像编辑 | Venice API 文档"
"og:description": "介绍 Venice 图像编辑（也称 inpaint）API 的文档。"
---

<Warning>
这是一个实验性端点，可能会发生变更。
</Warning>

<Info>
**定价：** 图像编辑的定价因模型而异。默认模型（`qwen-edit`）为**每次编辑 0.04 美元**。请查看 [Models 端点](/zh/api-reference/endpoint/models/list)以获取所有可用编辑模型及其定价。
</Info>

## 质量层级

部分编辑模型接受可选的 `quality` 参数（`low`、`medium`、`high`），用于在视觉保真度和成本之间权衡。当前由 `gpt-image-2-edit` 支持；其他模型会忽略该参数。

```json
{
  "model": "gpt-image-2-edit",
  "image": "iVBORw0KGgo...",
  "prompt": "change the sky to a sunrise",
  "resolution": "2K",
  "quality": "medium"
}
```

当你省略 `quality` 时，模型使用其默认层级（`gpt-image-2-edit` 为 `high`）。各层级价格（1K/2K/4K × low/medium/high）位于 [Models 端点](/zh/api-reference/endpoint/models/list) 中 `model_spec.pricing.quality` 字段下，并在[定价概览](/zh/overview/pricing)中列出。

<Info>
质量感知的定价对所有 API 和 SDK 调用方都是默认行为 —— 无需选择加入的请求头。未传 `quality` 的请求按模型的默认层级计费（`gpt-image-2-edit` 为 `high`）。
</Info>

## Postman 集合
如需更多示例，请参阅此 [Postman Collection](https://www.postman.com/veniceai/workspace/venice-ai-workspace/folder/38652128-2d156cd6-a9bc-4586-8a8b-98e4b5c4435d?action=share&source=copy-link&creator=38652128&ctx=documentation)。

-------

<Warning>
默认模型（`qwen-edit`）会阻止生成露骨性内容、性化未成年人或描绘真实世界暴力的请求。其他模型可能有不同的内容策略。
</Warning>

