---
title: Venice API
"og:title": "Venice API 文档"
mode: "wide"
---

<div className="venice-hero-copy">
  <p className="venice-hero-tagline">私密、无限制地访问智能的 API。</p>
  <p className="venice-hero-subtitle">兼容 OpenAI 的聊天、图像、音频与视频，统一在一个 API 密钥之下。</p>
  <div className="venice-hero-ctas">
    <a className="venice-hero-cta venice-hero-cta-primary" href="https://venice.ai/settings/api">获取 API 密钥 →</a>
    <a className="venice-hero-cta" href="/zh/overview/getting-started">开始使用</a>
  </div>
</div>

<CodeGroup>
```bash curl
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zai-org-glm-5-1",
    "messages": [{"role": "user", "content": "Build without permission."}]
  }'
```

```ts TypeScript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY,
  baseURL: "https://api.venice.ai/api/v1",
});

const res = await client.chat.completions.create({
  model: "zai-org-glm-5-1",
  messages: [{ role: "user", content: "Build without permission." }],
});
```

```python Python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

res = client.chat.completions.create(
    model="zai-org-glm-5-1",
    messages=[{"role": "user", "content": "Build without permission."}],
)
```
</CodeGroup>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">端点</p>
  <h2 className="venice-section-title">一个 API，覆盖每一种模态</h2>
  <p className="venice-section-subtitle">聊天、图像、音频、视频与嵌入，统一在一个 API 密钥之下。</p>
</div>

<div className="venice-apis-grid">
  <a className="venice-api-card" href="/zh/api-reference/endpoint/chat/completions">
    <span className="venice-api-card-name">聊天补全</span>
    <p className="venice-api-card-desc">兼容 OpenAI 的聊天，支持推理、工具使用与流式传输，覆盖 100+ 文本模型。</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">流式</span>
      <span className="venice-api-card-chip">工具</span>
      <span className="venice-api-card-chip">视觉</span>
    </div>
    <span className="venice-api-card-action">查看参考 →</span>
  </a>

  <a className="venice-api-card" href="/zh/api-reference/endpoint/image/generations">
    <span className="venice-api-card-name">图像生成</span>
    <p className="venice-api-card-desc">文本到图像、图像到图像、放大与背景移除，覆盖写实、风格化与未审查模型。</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">文本到图像</span>
      <span className="venice-api-card-chip">图像到图像</span>
      <span className="venice-api-card-chip">放大</span>
    </div>
    <span className="venice-api-card-action">查看参考 →</span>
  </a>

  <a className="venice-api-card" href="/zh/api-reference/endpoint/audio/speech">
    <span className="venice-api-card-name">音频</span>
    <p className="venice-api-card-desc">支持 50+ 多语言语音的文本转语音，以及适用于任何音频文件的语音转文本转录。</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">TTS</span>
      <span className="venice-api-card-chip">转录</span>
      <span className="venice-api-card-chip">50+ 语音</span>
    </div>
    <span className="venice-api-card-action">查看参考 →</span>
  </a>

  <a className="venice-api-card" href="/zh/api-reference/endpoint/video/queue">
    <span className="venice-api-card-name">视频</span>
    <p className="venice-api-card-desc">通过同步或异步任务队列实现文本到视频、图像到视频与参考到视频。</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">文本到视频</span>
      <span className="venice-api-card-chip">图像到视频</span>
      <span className="venice-api-card-chip">参考到视频</span>
    </div>
    <span className="venice-api-card-action">查看参考 →</span>
  </a>
</div>

<p className="venice-apis-extras">
  此外还有 <a href="/zh/api-reference/endpoint/embeddings/generate">嵌入</a>、<a href="/zh/guides/features/file-inputs">文件输入</a>、<a href="/zh/guides/integrations/venice-mcp">MCP 工具</a> 与 <a href="/zh/guides/integrations/x402-venice-api">钱包支付</a>。<a className="venice-apis-extras-cta" href="/zh/api-reference">查看所有端点 →</a>
</p>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">代理</p>
  <h2 className="venice-section-title">为 AI 代理而构建</h2>
  <p className="venice-section-subtitle">为消息、编码与链上代理提供私有推理、MCP 工具和钱包资助的工作流。</p>
</div>

<div className="venice-agents-cards">
  <a className="venice-api-card" href="/zh/guides/integrations/ai-agents#agent-apps">
    <span className="venice-api-card-name">代理应用</span>
    <p className="venice-api-card-desc">通过 OpenClaw、Hermes 和 NanoClaw 将 Venice 连接到 WhatsApp、Telegram、Discord 等。</p>
    <span className="venice-api-card-action">查看集成 →</span>
  </a>
  <a className="venice-api-card" href="/zh/guides/integrations/ai-agents#coding-agents">
    <span className="venice-api-card-name">编码代理</span>
    <p className="venice-api-card-desc">将 Claude Code、Cursor 与 Codex CLI 与 Venice 模型结合，实现私密的编码工作流。</p>
    <span className="venice-api-card-action">查看集成 →</span>
  </a>
  <a className="venice-api-card" href="/zh/guides/integrations/ai-agents#tools-and-skills">
    <span className="venice-api-card-name">MCP + 技能</span>
    <p className="venice-api-card-desc">将聊天、图像、视频、音频与嵌入作为 MCP 工具或运行时技能暴露出来。</p>
    <span className="venice-api-card-action">查看集成 →</span>
  </a>
</div>

<p className="venice-apis-extras">
  <a className="venice-apis-extras-cta" href="/zh/guides/integrations/ai-agents">探索 AI 代理中心 →</a>
</p>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">模型</p>
  <h2 className="venice-section-title">热门模型</h2>
  <p className="venice-section-subtitle">在 Venice 上使用最广泛的几款模型。将 ID 作为你的 `model` 参数即可。</p>
</div>

<div className="venice-models-grid">
  <a className="venice-model-card" href="/zh/overview/models">
    <div className="venice-model-card-head">
      <span className="venice-model-card-name">Kimi K2.6</span>
      <span className="venice-model-card-maker">Moonshot AI</span>
    </div>
    <p className="venice-model-card-desc">开源权重的前沿推理模型。以远低于前沿模型的价格提供强大的长上下文与工具使用能力。</p>
    <div className="venice-model-card-stats">
      <span>256K 上下文</span>
      <span>\$0.85 / \$4.66 每 1M</span>
      <span className="venice-model-card-privacy">Private</span>
    </div>
    <code className="venice-model-card-id">kimi-k2-6</code>
  </a>

  <a className="venice-model-card" href="/zh/overview/models">
    <div className="venice-model-card-head">
      <span className="venice-model-card-name">Claude Opus 4.7</span>
      <span className="venice-model-card-maker">Anthropic</span>
    </div>
    <p className="venice-model-card-desc">在编码、规划与需要保持连贯的长跨度代理任务上业内领先。</p>
    <div className="venice-model-card-stats">
      <span>1M 上下文</span>
      <span>\$6.00 / \$30.00 每 1M</span>
      <span className="venice-model-card-privacy">Anonymized</span>
    </div>
    <code className="venice-model-card-id">claude-opus-4-7</code>
  </a>

  <a className="venice-model-card" href="/zh/overview/models">
    <div className="venice-model-card-head">
      <span className="venice-model-card-name">GPT-5.5</span>
      <span className="venice-model-card-maker">OpenAI</span>
    </div>
    <p className="venice-model-card-desc">具备 1M 上下文的前沿通用智能模型。适合作为聊天、RAG 与多步推理的可靠默认选择。</p>
    <div className="venice-model-card-stats">
      <span>1M 上下文</span>
      <span>\$6.25 / \$37.50 每 1M</span>
      <span className="venice-model-card-privacy">Anonymized</span>
    </div>
    <code className="venice-model-card-id">openai-gpt-55</code>
  </a>
</div>

<a className="venice-models-cta" href="/zh/overview/models">
  <span className="venice-models-cta-left">
    <span className="venice-models-cta-count">250+ 个模型</span>
    <span className="venice-models-cta-sub">文本、图像、音频与视频</span>
  </span>
  <span className="venice-models-cta-action">浏览目录 →</span>
</a>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">工具</p>
  <h2 className="venice-section-title">聊天模型的内置工具</h2>
  <p className="venice-section-subtitle">通过 `venice_parameters` 或 Venice 原生端点开启网页搜索、附加文件或查询区块链。</p>
</div>

<CardGroup cols={4}>
  <Card title="网页搜索" icon="globe">
  </Card>

  <Card title="网页抓取" icon="browser">
  </Card>

  <Card title="文件输入" icon="file">
  </Card>

  <Card title="加密 RPC" icon="link">
  </Card>
</CardGroup>

<Accordion title="网页搜索代码示例">
通过 `enable_web_search` 为任意文本模型添加带引用的实时网页搜索。

<CodeGroup>
```bash Curl
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zai-org-glm-5-1",
    "messages": [{"role": "user", "content": "What are the latest developments in AI?"}],
    "venice_parameters": {
      "enable_web_search": "auto"
    }
  }'
```

```ts TypeScript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY!,
  baseURL: "https://api.venice.ai/api/v1",
});

const completion = await client.chat.completions.create({
  model: "zai-org-glm-5-1",
  messages: [{ role: "user", content: "What are the latest developments in AI?" }],
  // @ts-expect-error - Venice-specific parameter
  venice_parameters: {
    enable_web_search: "auto",
  },
});

console.log(completion.choices[0].message.content);
```

```python Python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

response = client.chat.completions.create(
    model="zai-org-glm-5-1",
    messages=[{"role": "user", "content": "What are the latest developments in AI?"}],
    extra_body={
        "venice_parameters": {
            "enable_web_search": "auto",
        }
    },
)

print(response.choices[0].message.content)
```

```bash Model Suffix
# 备选方案：将参数直接附加到模型 ID 上
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zai-org-glm-5-1:enable_web_search=on&enable_web_citations=true",
    "messages": [{"role": "user", "content": "What are the latest developments in AI?"}]
  }'
```
</CodeGroup>
</Accordion>

<Accordion title="网页抓取代码示例">
设置 `enable_web_scraping: true`，模型会在回答前抓取并阅读用户消息中的任意 URL。

<CodeGroup>
```bash Curl
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "openai-gpt-55",
    "messages": [
      {"role": "user", "content": "Summarize this post in five bullets: https://venice.ai/blog/how-to-use-venice-api"}
    ],
    "venice_parameters": {
      "enable_web_scraping": true
    }
  }'
```

```ts TypeScript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY!,
  baseURL: "https://api.venice.ai/api/v1",
});

const response = await client.chat.completions.create({
  model: "openai-gpt-55",
  messages: [
    {
      role: "user",
      content:
        "Summarize this post in five bullets: https://venice.ai/blog/how-to-use-venice-api",
    },
  ],
  // @ts-expect-error - Venice-specific parameter
  venice_parameters: {
    enable_web_scraping: true,
  },
});

console.log(response.choices[0].message.content);
```

```python Python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

response = client.chat.completions.create(
    model="openai-gpt-55",
    messages=[
        {
            "role": "user",
            "content": "Summarize this post in five bullets: https://venice.ai/blog/how-to-use-venice-api",
        }
    ],
    extra_body={
        "venice_parameters": {
            "enable_web_scraping": True,
        }
    },
)

print(response.choices[0].message.content)
```
</CodeGroup>
</Accordion>

<Accordion title="文件输入代码示例">
将 PDF、Office 文档、代码和文本文件（最大 25MB）直接附加到聊天请求中。完整格式列表见[文件输入指南](/zh/guides/features/file-inputs)。

<CodeGroup>
```bash Curl
# 将本地文件编码为 base64 data URL，然后内联发送
FILE_B64=$(base64 q3-report.pdf | tr -d '\n')

curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d "{
    \"model\": \"openai-gpt-55\",
    \"messages\": [
      {
        \"role\": \"user\",
        \"content\": [
          {\"type\": \"text\", \"text\": \"Summarize this report in five bullets and list the main risks.\"},
          {\"type\": \"file\", \"file\": {\"filename\": \"q3-report.pdf\", \"file_data\": \"data:application/pdf;base64,${FILE_B64}\"}}
        ]
      }
    ]
  }"
```

```ts TypeScript
import OpenAI from "openai";
import { readFile } from "node:fs/promises";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY!,
  baseURL: "https://api.venice.ai/api/v1",
});

const pdf = await readFile("q3-report.pdf");
const fileData = `data:application/pdf;base64,${pdf.toString("base64")}`;

const response = await client.chat.completions.create({
  model: "openai-gpt-55",
  messages: [
    {
      role: "user",
      content: [
        { type: "text", text: "Summarize this report in five bullets and list the main risks." },
        // @ts-expect-error - Venice file input block
        { type: "file", file: { filename: "q3-report.pdf", file_data: fileData } },
      ],
    },
  ],
});

console.log(response.choices[0].message.content);
```

```python Python
import base64
import os
from pathlib import Path
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

path = Path("q3-report.pdf")
file_data = "data:application/pdf;base64," + base64.b64encode(path.read_bytes()).decode("utf-8")

response = client.chat.completions.create(
    model="openai-gpt-55",
    messages=[
        {
            "role": "user",
            "content": [
                {"type": "text", "text": "Summarize this report in five bullets and list the main risks."},
                {"type": "file", "file": {"filename": "q3-report.pdf", "file_data": file_data}},
            ],
        }
    ],
)

print(response.choices[0].message.content)
```
</CodeGroup>
</Accordion>

<Accordion title="加密 RPC 代码示例">
使用 Venice 密钥或 x402 钱包，在 11 条受支持的链上代理 JSON-RPC 2.0 调用。链、方法与 credit 层级详见[加密 RPC 参考](/zh/api-reference/endpoint/crypto/rpc)。

<CodeGroup>
```bash Curl
curl https://api.venice.ai/api/v1/crypto/rpc/ethereum-mainnet \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_blockNumber",
    "params": [],
    "id": 1
  }'
```

```ts TypeScript
const response = await fetch(
  "https://api.venice.ai/api/v1/crypto/rpc/base-mainnet",
  {
    method: "POST",
    headers: {
      Authorization: `Bearer ${process.env.VENICE_API_KEY}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify([
      { jsonrpc: "2.0", method: "eth_chainId", params: [], id: 1 },
      { jsonrpc: "2.0", method: "eth_blockNumber", params: [], id: 2 },
    ]),
  }
);

const results = await response.json();
console.log(results);
```

```python Python
import os
import requests

response = requests.post(
    "https://api.venice.ai/api/v1/crypto/rpc/ethereum-mainnet",
    headers={
        "Authorization": f"Bearer {os.environ['VENICE_API_KEY']}",
        "Content-Type": "application/json",
    },
    json={
        "jsonrpc": "2.0",
        "method": "eth_getBalance",
        "params": ["0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045", "latest"],
        "id": 1,
    },
)

print(response.json())
```
</CodeGroup>
</Accordion>

<div className="venice-pricing">
  <div className="venice-section-header">
    <p className="venice-section-eyebrow">定价</p>
    <h2 className="venice-section-title">充值、质押或按请求付费</h2>
    <p className="venice-section-subtitle">使用 credit 为账户充值、质押 DIEM 获取每日额度，或直接用 Base 上的 USDC 跳过账户。</p>
  </div>

  <div className="venice-pricing-grid">
    <div className="venice-pricing-card">
      <div className="venice-pricing-card-head">
        <span className="venice-pricing-card-name">Credits</span>
        <span className="venice-pricing-card-badge">USD 或加密货币</span>
      </div>
      <p className="venice-pricing-card-desc">以 USD 或加密货币按用量付费。Credit 永不过期，适用于所有端点。</p>
      <a className="venice-pricing-card-cta" href="https://venice.ai/settings/billing">购买 Credit</a>
    </div>

    <div className="venice-pricing-card">
      <div className="venice-pricing-card-head">
        <span className="venice-pricing-card-name">DIEM</span>
        <span className="venice-pricing-card-badge">每日额度</span>
      </div>
      <p className="venice-pricing-card-desc">一次性质押 DIEM 或 VVV，每天获得固定推理额度，无按次费用。</p>
      <a className="venice-pricing-card-cta" href="https://venice.ai/token">了解 DIEM</a>
    </div>

    <div className="venice-pricing-card">
      <div className="venice-pricing-card-head">
        <span className="venice-pricing-card-name">x402</span>
        <span className="venice-pricing-card-badge">Base 上的 USDC</span>
      </div>
      <p className="venice-pricing-card-desc">从任意 Base 钱包用 USDC 按请求付费。无需账户或 API 密钥，专为代理而构建。</p>
      <a className="venice-pricing-card-cta" href="/zh/guides/integrations/x402-venice-api">阅读 x402 指南</a>
    </div>
  </div>
</div>

有问题或反馈？欢迎加入我们的 [Discord](https://discord.gg/askvenice)。
