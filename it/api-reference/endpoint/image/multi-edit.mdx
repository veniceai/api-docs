---
title: 'Multi-Edit immagini'
openapi: 'POST /image/multi-edit'
"og:title": "Multi-Edit Image | Venice API Docs"
"og:description": "Modifica immagini usando fino a tre input stratificati (immagine base più mask/overlay)."
---

<Info>
**Prezzo:** Il prezzo del multi-edit varia per modello. Consulta la [panoramica dei prezzi](/it/overview/pricing)
per i prezzi correnti per ogni modifica.
</Info>

## Fasce di qualità

Alcuni modelli multi-edit accettano un parametro opzionale `quality` (`low`, `medium`, `high`) che bilancia fedeltà visiva e costo. Attualmente supportato da `gpt-image-2-edit`; gli altri modelli ignorano il parametro.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

Quando ometti `quality`, il modello usa la sua fascia predefinita (`high` per `gpt-image-2-edit`). I prezzi per fascia (1K/2K/4K × low/medium/high) sono esposti sotto `model_spec.pricing.quality` nell'[endpoint Models](/it/api-reference/endpoint/models/list) e sono elencati nella [panoramica dei prezzi](/it/overview/pricing).

<Info>
Il pricing basato sulla qualità è l'impostazione predefinita per tutti i chiamanti API e SDK — non è richiesto alcun header opt-in. Le richieste che omettono `quality` vengono fatturate alla fascia predefinita del modello (`high` per `gpt-image-2-edit`).
</Info>

