---
title: Codex CLI
description: Usa OpenAI Codex CLI con i modelli Venice AI tramite un file di configurazione config.toml locale
"og:title": "Codex CLI with Venice | Venice API Docs"
"og:description": "Configura Codex CLI per inviare richieste a Venice AI usando una semplice configurazione del provider del modello"
---

Questa guida mostra come eseguire OpenAI Codex CLI con Venice usando i percorsi di configurazione ufficiali di Codex: `~/.codex/config.toml` (a livello utente) o `.codex/config.toml` (a livello di progetto).

<CardGroup cols={3}>
  <Card title="Configurazione semplice" icon="gear">
    Un file di configurazione nel tuo progetto
  </Card>
  <Card title="Compatibile OpenAI" icon="plug">
    Usa l'API compatibile OpenAI di Venice
  </Card>
  <Card title="Flessibilità del modello" icon="microchip">
    Sostituisci con qualsiasi modello di testo Venice supportato
  </Card>
</CardGroup>

---

## Prerequisiti

- Una API key Venice da [venice.ai/settings/api](https://venice.ai/settings/api)
- Codex CLI installato e funzionante sulla tua macchina

---

## Configurazione

<Steps>
  <Step title="Crea il percorso di configurazione del progetto">
    Dalla root del tuo progetto:

    ```bash
    mkdir -p .codex
    ```
  </Step>

  <Step title="Crea .codex/config.toml">
    Crea il file e incolla la configurazione seguente:

    ```toml
    #:schema https://developers.openai.com/codex/config-schema.json

    model = "openai-gpt-54" # use any Venice model
    model_provider = "venice"
    model_reasoning_effort = "high"
    personality = "pragmatic"
    sandbox_mode = "workspace-write"

    [model_providers.venice]
    name = "Venice"
    base_url = "https://api.venice.ai/api/v1/"
    experimental_bearer_token = "YOUR VENICE API KEY"
    wire_api = "responses"
    ```
  </Step>

  <Step title="Sostituisci i due placeholder">
    Aggiorna:

    - `model` con l'ID del modello Venice che vuoi usare
    - `experimental_bearer_token` con la tua vera API key Venice

    Puoi sfogliare gli ID modello disponibili nel [catalogo modelli di testo](/it/models/text).
  </Step>

  <Step title="Esegui Codex CLI normalmente">
    Avvia Codex CLI dallo stesso progetto. Caricherà `.codex/config.toml` (per progetti trusted) e instraderà le richieste tramite Venice.
  </Step>
</Steps>

---

## Percorsi di configurazione ufficiali di Codex

- **Default utente**: `~/.codex/config.toml`
- **Override di progetto**: `.codex/config.toml` (caricato solo per progetti trusted)

Se vuoi che le impostazioni Venice si applichino ovunque, metti la stessa configurazione in `~/.codex/config.toml`.

---

## Precedenza della configurazione (dalla più alta)

1. Flag CLI e override `--config`
2. Valori del profilo (`--profile <name>`)
3. Livelli di configurazione di progetto (`.codex/config.toml`, vince la directory più vicina)
4. Configurazione utente (`~/.codex/config.toml`)
5. Configurazione di sistema (`/etc/codex/config.toml`, Unix)
6. Default integrati

---

## Note

- Mantieni privata la tua API key e non committare mai key reali su git.
- Codex ignora la configurazione di progetto `.codex/` quando un progetto è marcato come untrusted.
- Se cambi modello, aggiorna solo il campo `model`.
- L'impostazione `wire_api = "responses"` è richiesta per questa configurazione del provider.

---

## Risorse

<CardGroup cols={2}>
  <Card title="Riferimento API Venice" icon="book" href="/it/api-reference/api-spec">
    Documentazione completa di endpoint e parametri
  </Card>
  <Card title="Modelli di testo Venice" icon="list" href="/it/models/text">
    ID modello disponibili
  </Card>
</CardGroup>
