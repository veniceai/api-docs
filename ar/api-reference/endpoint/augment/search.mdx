---
title: 'البحث في الويب'
openapi: 'POST /augment/search'
"og:title": "Web Search | Venice API Docs"
"og:description": "توثيق يغطي واجهة البحث في الويب من Venice لاسترجاع نتائج بحث مهيكلة مع موفّرين يحفظون الخصوصية."
---

<Warning>هذه واجهة API تجريبية. قد يتغير تنسيق الطلب والاستجابة دون إشعار.</Warning>

أرسل استعلام البحث في حقل `query`. تُعيد الـ API نتائج مهيكلة تشمل العناوين وعناوين URL ومقتطفات المحتوى والتواريخ.

**موفّرو البحث:**
- `brave` (الافتراضي) — Brave Search مع عدم الاحتفاظ بالبيانات (ZDR). لا يخزّن موفّر البحث استعلامات البحث ولا يسجّلها أبدًا.
- `google` — Google Search باستعلامات مجهولة الهوية. يتم تمرير عمليات البحث عبر بنية Venice التحتية بحيث لا تُربط هويتك بطلب البحث المُرسَل إلى Google. لا تخزّن Venice استعلامات البحث ولا تسجّلها.

**التسعير:** $0.01 لكل طلب.

### مثال (cURL)

```bash
curl -X POST https://api.venice.ai/api/v1/augment/search \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"query": "latest news about AI"}'
```

-------
