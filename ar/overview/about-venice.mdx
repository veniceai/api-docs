---
title: Venice API
"og:title": "مستندات Venice API"
mode: "wide"
---

<div className="venice-hero-copy">
  <p className="venice-hero-tagline">الـ API للوصول الخاص وغير المقيَّد إلى الذكاء.</p>
  <p className="venice-hero-subtitle">دردشة وصور وصوت وفيديو متوافقة مع OpenAI خلف مفتاح API واحد.</p>
  <div className="venice-hero-ctas">
    <a className="venice-hero-cta venice-hero-cta-primary" href="https://venice.ai/settings/api">احصل على مفتاح API ←</a>
    <a className="venice-hero-cta" href="/ar/overview/getting-started">ابدأ الآن</a>
  </div>
</div>

<CodeGroup>
```bash curl
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zai-org-glm-5-1",
    "messages": [{"role": "user", "content": "Build without permission."}]
  }'
```

```ts TypeScript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY,
  baseURL: "https://api.venice.ai/api/v1",
});

const res = await client.chat.completions.create({
  model: "zai-org-glm-5-1",
  messages: [{ role: "user", content: "Build without permission." }],
});
```

```python Python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

res = client.chat.completions.create(
    model="zai-org-glm-5-1",
    messages=[{"role": "user", "content": "Build without permission."}],
)
```
</CodeGroup>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">نقاط النهاية</p>
  <h2 className="venice-section-title">API واحد لكل نمط</h2>
  <p className="venice-section-subtitle">دردشة، صور، صوت، فيديو، وembeddings خلف مفتاح API واحد.</p>
</div>

<div className="venice-apis-grid">
  <a className="venice-api-card" href="/ar/api-reference/endpoint/chat/completions">
    <span className="venice-api-card-name">Chat Completions</span>
    <p className="venice-api-card-desc">دردشة متوافقة مع OpenAI مع التفكير واستخدام الأدوات والبث عبر أكثر من 100 نموذج نصي.</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">بث</span>
      <span className="venice-api-card-chip">أدوات</span>
      <span className="venice-api-card-chip">رؤية</span>
    </div>
    <span className="venice-api-card-action">انظر المرجع ←</span>
  </a>

  <a className="venice-api-card" href="/ar/api-reference/endpoint/image/generations">
    <span className="venice-api-card-name">Image Generation</span>
    <p className="venice-api-card-desc">تحويل النص إلى صورة، والصورة إلى صورة، ورفع الدقة، وإزالة الخلفية عبر نماذج واقعية ومنمَّطة وغير مُقيَّدة.</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">نص إلى صورة</span>
      <span className="venice-api-card-chip">صورة إلى صورة</span>
      <span className="venice-api-card-chip">رفع الدقة</span>
    </div>
    <span className="venice-api-card-action">انظر المرجع ←</span>
  </a>

  <a className="venice-api-card" href="/ar/api-reference/endpoint/audio/speech">
    <span className="venice-api-card-name">Audio</span>
    <p className="venice-api-card-desc">تحويل النص إلى كلام بأكثر من 50 صوتًا متعدد اللغات، بالإضافة إلى تحويل الكلام إلى نص لأي ملف صوتي.</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">TTS</span>
      <span className="venice-api-card-chip">تفريغ</span>
      <span className="venice-api-card-chip">+50 صوتًا</span>
    </div>
    <span className="venice-api-card-action">انظر المرجع ←</span>
  </a>

  <a className="venice-api-card" href="/ar/api-reference/endpoint/video/queue">
    <span className="venice-api-card-name">Video</span>
    <p className="venice-api-card-desc">تحويل النص إلى فيديو، والصورة إلى فيديو، والمرجع إلى فيديو عبر قائمة مهام متزامنة أو غير متزامنة.</p>
    <div className="venice-api-card-chips">
      <span className="venice-api-card-chip">نص إلى فيديو</span>
      <span className="venice-api-card-chip">صورة إلى فيديو</span>
      <span className="venice-api-card-chip">مرجع إلى فيديو</span>
    </div>
    <span className="venice-api-card-action">انظر المرجع ←</span>
  </a>
</div>

<p className="venice-apis-extras">
  بالإضافة إلى <a href="/ar/api-reference/endpoint/embeddings/generate">embeddings</a>، و<a href="/ar/guides/features/file-inputs">مدخلات الملفات</a>، و<a href="/ar/guides/integrations/venice-mcp">أدوات MCP</a>، و<a href="/ar/guides/integrations/x402-venice-api">المدفوعات عبر المحفظة</a>. <a className="venice-apis-extras-cta" href="/ar/api-reference">عرض جميع نقاط النهاية ←</a>
</p>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">الوكلاء</p>
  <h2 className="venice-section-title">مُصمَّم لوكلاء الذكاء الاصطناعي</h2>
  <p className="venice-section-subtitle">استدلال خاص، وأدوات MCP، وتدفقات عمل مموَّلة من المحفظة لوكلاء الرسائل والبرمجة والوكلاء على السلسلة.</p>
</div>

<div className="venice-agents-cards">
  <a className="venice-api-card" href="/ar/guides/integrations/ai-agents#agent-apps">
    <span className="venice-api-card-name">تطبيقات الوكلاء</span>
    <p className="venice-api-card-desc">اربط Venice بـ WhatsApp وTelegram وDiscord والمزيد عبر OpenClaw وHermes وNanoClaw.</p>
    <span className="venice-api-card-action">انظر التكاملات ←</span>
  </a>
  <a className="venice-api-card" href="/ar/guides/integrations/ai-agents#coding-agents">
    <span className="venice-api-card-name">وكلاء البرمجة</span>
    <p className="venice-api-card-desc">استخدم Claude Code وCursor وCodex CLI مع نماذج Venice لتدفقات برمجة خاصة.</p>
    <span className="venice-api-card-action">انظر التكاملات ←</span>
  </a>
  <a className="venice-api-card" href="/ar/guides/integrations/ai-agents#tools-and-skills">
    <span className="venice-api-card-name">MCP + Skills</span>
    <p className="venice-api-card-desc">اعرض الدردشة والصور والفيديو والصوت وembeddings كأدوات MCP أو مهارات تشغيلية.</p>
    <span className="venice-api-card-action">انظر التكاملات ←</span>
  </a>
</div>

<p className="venice-apis-extras">
  <a className="venice-apis-extras-cta" href="/ar/guides/integrations/ai-agents">استكشف مركز وكلاء الذكاء الاصطناعي ←</a>
</p>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">النماذج</p>
  <h2 className="venice-section-title">النماذج الشائعة</h2>
  <p className="venice-section-subtitle">بعض من أكثر النماذج استخدامًا على Venice. استخدم المعرّف كقيمة لمعامل `model` لديك.</p>
</div>

<div className="venice-models-grid">
  <a className="venice-model-card" href="/ar/overview/models">
    <div className="venice-model-card-head">
      <span className="venice-model-card-name">Kimi K2.6</span>
      <span className="venice-model-card-maker">Moonshot AI</span>
    </div>
    <p className="venice-model-card-desc">تفكير متقدّم بأوزان مفتوحة. سياق طويل قوي واستخدام للأدوات بجزء يسير من أسعار النماذج المتقدمة.</p>
    <div className="venice-model-card-stats">
      <span>سياق 256K</span>
      <span>\$0.85 / \$4.66 لكل 1M</span>
      <span className="venice-model-card-privacy">Private</span>
    </div>
    <code className="venice-model-card-id">kimi-k2-6</code>
  </a>

  <a className="venice-model-card" href="/ar/overview/models">
    <div className="venice-model-card-head">
      <span className="venice-model-card-name">Claude Opus 4.7</span>
      <span className="venice-model-card-maker">Anthropic</span>
    </div>
    <p className="venice-model-card-desc">الأفضل في فئته للبرمجة والتخطيط ووكلاء الأفق الطويل الذين يحتاجون إلى التماسك.</p>
    <div className="venice-model-card-stats">
      <span>سياق 1M</span>
      <span>\$6.00 / \$30.00 لكل 1M</span>
      <span className="venice-model-card-privacy">Anonymized</span>
    </div>
    <code className="venice-model-card-id">claude-opus-4-7</code>
  </a>

  <a className="venice-model-card" href="/ar/overview/models">
    <div className="venice-model-card-head">
      <span className="venice-model-card-name">GPT-5.5</span>
      <span className="venice-model-card-maker">OpenAI</span>
    </div>
    <p className="venice-model-card-desc">ذكاء عام متقدّم بسياق 1M. خيار افتراضي قوي للدردشة وRAG والتفكير متعدد الخطوات.</p>
    <div className="venice-model-card-stats">
      <span>سياق 1M</span>
      <span>\$6.25 / \$37.50 لكل 1M</span>
      <span className="venice-model-card-privacy">Anonymized</span>
    </div>
    <code className="venice-model-card-id">openai-gpt-55</code>
  </a>
</div>

<a className="venice-models-cta" href="/ar/overview/models">
  <span className="venice-models-cta-left">
    <span className="venice-models-cta-count">أكثر من 250 نموذجًا</span>
    <span className="venice-models-cta-sub">نص، صور، صوت، وفيديو</span>
  </span>
  <span className="venice-models-cta-action">تصفّح الكتالوج ←</span>
</a>

<div className="venice-section-header">
  <p className="venice-section-eyebrow">الأدوات</p>
  <h2 className="venice-section-title">أدوات مدمجة لنماذج الدردشة</h2>
  <p className="venice-section-subtitle">فعِّل البحث على الويب، أو أرفِق ملفات، أو استعلم من سلسلة كتل عبر `venice_parameters` أو نقطة نهاية أصيلة في Venice.</p>
</div>

<CardGroup cols={4}>
  <Card title="البحث على الويب" icon="globe">
  </Card>

  <Card title="استخراج الويب" icon="browser">
  </Card>

  <Card title="مدخلات الملفات" icon="file">
  </Card>

  <Card title="Crypto RPC" icon="link">
  </Card>
</CardGroup>

<Accordion title="أمثلة كود البحث على الويب">
أضِف بحثًا في الويب في الوقت الفعلي مع الاستشهادات لأي نموذج نصي عبر `enable_web_search`.

<CodeGroup>
```bash Curl
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zai-org-glm-5-1",
    "messages": [{"role": "user", "content": "What are the latest developments in AI?"}],
    "venice_parameters": {
      "enable_web_search": "auto"
    }
  }'
```

```ts TypeScript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY!,
  baseURL: "https://api.venice.ai/api/v1",
});

const completion = await client.chat.completions.create({
  model: "zai-org-glm-5-1",
  messages: [{ role: "user", content: "What are the latest developments in AI?" }],
  // @ts-expect-error - Venice-specific parameter
  venice_parameters: {
    enable_web_search: "auto",
  },
});

console.log(completion.choices[0].message.content);
```

```python Python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

response = client.chat.completions.create(
    model="zai-org-glm-5-1",
    messages=[{"role": "user", "content": "What are the latest developments in AI?"}],
    extra_body={
        "venice_parameters": {
            "enable_web_search": "auto",
        }
    },
)

print(response.choices[0].message.content)
```

```bash Model Suffix
# بديل: ألحق المعلمات مباشرة بمعرّف النموذج
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zai-org-glm-5-1:enable_web_search=on&enable_web_citations=true",
    "messages": [{"role": "user", "content": "What are the latest developments in AI?"}]
  }'
```
</CodeGroup>
</Accordion>

<Accordion title="أمثلة كود استخراج الويب">
اضبط `enable_web_scraping: true` وسيقوم النموذج بجلب وقراءة أي روابط في رسالة المستخدم قبل الإجابة.

<CodeGroup>
```bash Curl
curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "openai-gpt-55",
    "messages": [
      {"role": "user", "content": "Summarize this post in five bullets: https://venice.ai/blog/how-to-use-venice-api"}
    ],
    "venice_parameters": {
      "enable_web_scraping": true
    }
  }'
```

```ts TypeScript
import OpenAI from "openai";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY!,
  baseURL: "https://api.venice.ai/api/v1",
});

const response = await client.chat.completions.create({
  model: "openai-gpt-55",
  messages: [
    {
      role: "user",
      content:
        "Summarize this post in five bullets: https://venice.ai/blog/how-to-use-venice-api",
    },
  ],
  // @ts-expect-error - Venice-specific parameter
  venice_parameters: {
    enable_web_scraping: true,
  },
});

console.log(response.choices[0].message.content);
```

```python Python
import os
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

response = client.chat.completions.create(
    model="openai-gpt-55",
    messages=[
        {
            "role": "user",
            "content": "Summarize this post in five bullets: https://venice.ai/blog/how-to-use-venice-api",
        }
    ],
    extra_body={
        "venice_parameters": {
            "enable_web_scraping": True,
        }
    },
)

print(response.choices[0].message.content)
```
</CodeGroup>
</Accordion>

<Accordion title="أمثلة كود مدخلات الملفات">
أرفق ملفات PDF ومستندات Office وكود وملفات نصية (حتى 25MB) مباشرة في طلب الدردشة. راجع [دليل مدخلات الملفات](/ar/guides/features/file-inputs) للحصول على القائمة الكاملة بالصيغ.

<CodeGroup>
```bash Curl
# رمّز ملفًا محليًا كعنوان data URL بترميز base64، ثم أرسله ضمن الطلب
FILE_B64=$(base64 q3-report.pdf | tr -d '\n')

curl https://api.venice.ai/api/v1/chat/completions \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d "{
    \"model\": \"openai-gpt-55\",
    \"messages\": [
      {
        \"role\": \"user\",
        \"content\": [
          {\"type\": \"text\", \"text\": \"Summarize this report in five bullets and list the main risks.\"},
          {\"type\": \"file\", \"file\": {\"filename\": \"q3-report.pdf\", \"file_data\": \"data:application/pdf;base64,${FILE_B64}\"}}
        ]
      }
    ]
  }"
```

```ts TypeScript
import OpenAI from "openai";
import { readFile } from "node:fs/promises";

const client = new OpenAI({
  apiKey: process.env.VENICE_API_KEY!,
  baseURL: "https://api.venice.ai/api/v1",
});

const pdf = await readFile("q3-report.pdf");
const fileData = `data:application/pdf;base64,${pdf.toString("base64")}`;

const response = await client.chat.completions.create({
  model: "openai-gpt-55",
  messages: [
    {
      role: "user",
      content: [
        { type: "text", text: "Summarize this report in five bullets and list the main risks." },
        // @ts-expect-error - Venice file input block
        { type: "file", file: { filename: "q3-report.pdf", file_data: fileData } },
      ],
    },
  ],
});

console.log(response.choices[0].message.content);
```

```python Python
import base64
import os
from pathlib import Path
from openai import OpenAI

client = OpenAI(
    api_key=os.environ["VENICE_API_KEY"],
    base_url="https://api.venice.ai/api/v1",
)

path = Path("q3-report.pdf")
file_data = "data:application/pdf;base64," + base64.b64encode(path.read_bytes()).decode("utf-8")

response = client.chat.completions.create(
    model="openai-gpt-55",
    messages=[
        {
            "role": "user",
            "content": [
                {"type": "text", "text": "Summarize this report in five bullets and list the main risks."},
                {"type": "file", "file": {"filename": "q3-report.pdf", "file_data": file_data}},
            ],
        }
    ],
)

print(response.choices[0].message.content)
```
</CodeGroup>
</Accordion>

<Accordion title="أمثلة كود Crypto RPC">
وكّل استدعاءات JSON-RPC 2.0 عبر 11 سلسلة مدعومة باستخدام مفتاح Venice الخاص بك أو محفظة x402. راجع [مرجع Crypto RPC](/ar/api-reference/endpoint/crypto/rpc) للسلاسل والطرق ومستويات الأرصدة.

<CodeGroup>
```bash Curl
curl https://api.venice.ai/api/v1/crypto/rpc/ethereum-mainnet \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_blockNumber",
    "params": [],
    "id": 1
  }'
```

```ts TypeScript
const response = await fetch(
  "https://api.venice.ai/api/v1/crypto/rpc/base-mainnet",
  {
    method: "POST",
    headers: {
      Authorization: `Bearer ${process.env.VENICE_API_KEY}`,
      "Content-Type": "application/json",
    },
    body: JSON.stringify([
      { jsonrpc: "2.0", method: "eth_chainId", params: [], id: 1 },
      { jsonrpc: "2.0", method: "eth_blockNumber", params: [], id: 2 },
    ]),
  }
);

const results = await response.json();
console.log(results);
```

```python Python
import os
import requests

response = requests.post(
    "https://api.venice.ai/api/v1/crypto/rpc/ethereum-mainnet",
    headers={
        "Authorization": f"Bearer {os.environ['VENICE_API_KEY']}",
        "Content-Type": "application/json",
    },
    json={
        "jsonrpc": "2.0",
        "method": "eth_getBalance",
        "params": ["0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045", "latest"],
        "id": 1,
    },
)

print(response.json())
```
</CodeGroup>
</Accordion>

<div className="venice-pricing">
  <div className="venice-section-header">
    <p className="venice-section-eyebrow">الأسعار</p>
    <h2 className="venice-section-title">اشحن، أو راهن، أو ادفع لكل طلب</h2>
    <p className="venice-section-subtitle">موّل حسابًا بالأرصدة، أو راهِن DIEM للحصول على مخصص يومي، أو تخطّ الحساب بالكامل باستخدام USDC على Base.</p>
  </div>

  <div className="venice-pricing-grid">
    <div className="venice-pricing-card">
      <div className="venice-pricing-card-head">
        <span className="venice-pricing-card-name">الأرصدة</span>
        <span className="venice-pricing-card-badge">USD أو عملات مشفّرة</span>
      </div>
      <p className="venice-pricing-card-desc">ادفع كما تستخدم بالدولار أو بالعملات المشفّرة. الأرصدة لا تنتهي صلاحيتها وتعمل عبر كل نقطة نهاية.</p>
      <a className="venice-pricing-card-cta" href="https://venice.ai/settings/billing">شراء أرصدة</a>
    </div>

    <div className="venice-pricing-card">
      <div className="venice-pricing-card-head">
        <span className="venice-pricing-card-name">DIEM</span>
        <span className="venice-pricing-card-badge">مخصص يومي</span>
      </div>
      <p className="venice-pricing-card-desc">راهن على DIEM أو VVV مرة واحدة واكسب مخصص استدلال ثابتًا كل يوم، دون رسوم لكل استدعاء.</p>
      <a className="venice-pricing-card-cta" href="https://venice.ai/token">تعرّف على DIEM</a>
    </div>

    <div className="venice-pricing-card">
      <div className="venice-pricing-card-head">
        <span className="venice-pricing-card-name">x402</span>
        <span className="venice-pricing-card-badge">USDC على Base</span>
      </div>
      <p className="venice-pricing-card-desc">ادفع لكل طلب من أي محفظة Base بـ USDC. لا حساب ولا مفتاح API، مصمَّم للوكلاء.</p>
      <a className="venice-pricing-card-cta" href="/ar/guides/integrations/x402-venice-api">اقرأ دليل x402</a>
    </div>
  </div>
</div>

أسئلة أو ملاحظات؟ انضم إلينا على [Discord](https://discord.gg/askvenice).
