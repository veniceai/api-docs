---
title: OpenCode
description: استخدم OpenCode مع Venice AI عبر مزوّد مخصّص متوافق مع OpenAI
"og:title": "OpenCode مع Venice | وثائق Venice API"
"og:description": "هيّئ OpenCode لإرسال طلبات وكيل البرمجة إلى Venice AI باستخدام مزوّد مخصّص"
---

[OpenCode](https://opencode.ai/) هو وكيل برمجة بالذكاء الاصطناعي للطرفية. تعمل Venice عبر تهيئة المزوّد المخصص في OpenCode باستخدام المحوّل المتوافق مع OpenAI وعنوان URL أساسي مخصّص.

<CardGroup cols={3}>
  <Card title="مزوّد مخصّص" icon="sliders">
    أضف Venice كمزوّد في `opencode.json`
  </Card>
  <Card title="متوافق مع OpenAI" icon="plug">
    يستخدم نقطة نهاية `/chat/completions` في Venice
  </Card>
  <Card title="مفتاح من البيئة" icon="key">
    يقرأ مفتاح API الخاص بك من `VENICE_API_KEY`
  </Card>
</CardGroup>

---

## المتطلبات الأساسية

- مفتاح Venice API من [venice.ai/settings/api](https://venice.ai/settings/api)
- OpenCode مثبَّت ويعمل على جهازك

---

## الإعداد

<Steps>
  <Step title="اضبط مفتاح Venice API الخاص بك">
    صدّر مفتاح Venice API في الـ shell الذي تشغّل فيه OpenCode:

    ```bash
    export VENICE_API_KEY="your-api-key"
    ```
  </Step>

  <Step title="أنشئ ملف تهيئة OpenCode">
    لإعداد خاص بالمشروع، أنشئ `opencode.json` في جذر مشروعك.

    لإعداد عالمي، استخدم `~/.config/opencode/opencode.json` بدلاً من ذلك.
  </Step>

  <Step title="أضف مزوّد Venice">
    الصق هذه التهيئة في `opencode.json`:

    ```json
    {
      "$schema": "https://opencode.ai/config.json",
      "model": "venice/zai-org-glm-5-1",
      "small_model": "venice/zai-org-glm-5-1",
      "provider": {
        "venice": {
          "npm": "@ai-sdk/openai-compatible",
          "name": "Venice AI",
          "options": {
            "baseURL": "https://api.venice.ai/api/v1",
            "apiKey": "{env:VENICE_API_KEY}"
          },
          "models": {
            "zai-org-glm-5-1": {
              "name": "GLM 5.1"
            },
            "kimi-k2-5": {
              "name": "Kimi K2.5"
            },
            "minimax-m25": {
              "name": "MiniMax M2.5"
            }
          }
        }
      }
    }
    ```
  </Step>

  <Step title="شغّل OpenCode">
    شغّل OpenCode من نفس مجلد المشروع:

    ```bash
    opencode
    ```

    سيقوم OpenCode بتحميل `opencode.json`، واستخدام `VENICE_API_KEY`، وتوجيه نموذج Venice المحدد عبر `https://api.venice.ai/api/v1`.
  </Step>
</Steps>

---

## التحقق من الإعداد

داخل OpenCode، شغّل:

```txt
/models
```

يجب أن ترى نماذج Venice من تهيئتك. اختر واحداً، ثم أرسل طلباً اختبارياً صغيراً.

إذا ضبطت `"model": "venice/zai-org-glm-5-1"` كما هو موضح أعلاه، فيجب أن يستخدم OpenCode هذا النموذج من Venice افتراضياً. يمكنك تغيير الافتراضي باستبدال `zai-org-glm-5-1` بأي معرّف نموذج من [كتالوج نماذج النص](/ar/models/text) وإضافته إلى خريطة `models`.

---

## ملاحظات حول التهيئة

- اضبط `baseURL` على `https://api.venice.ai/api/v1`. يستخدم محوّل OpenCode المتوافق مع OpenAI عنوان URL الأساسي هذا لـ chat completions.
- اضبط `npm` على `@ai-sdk/openai-compatible` لنقطة نهاية دردشة Venice المتوافقة مع OpenAI.
- معرّف المزوّد هو `venice`، لذا تستخدم مراجع النماذج تنسيق `venice/<model-id>`.
- إذا كنت تفضّل تدفق `/connect` في OpenCode، اختر **Other**، استخدم `venice` كمعرّف للمزوّد، الصق مفتاح Venice API الخاص بك، وأزل `options.apiKey` من التهيئة.

<Info>
يدعم OpenCode بالفعل قيم `baseURL` المخصصة للمزوّد والمزوّدين المتوافقين مع OpenAI عبر `@ai-sdk/openai-compatible`. لا حاجة إلى أي تغييرات في مزوّد OpenCode الأساسي لـ Venice.
</Info>

---

## موارد

<CardGroup cols={2}>
  <Card title="نماذج Venice النصية" icon="list" href="/ar/models/text">
    معرّفات نماذج Venice المتاحة
  </Card>
  <Card title="وثائق مزوّدي OpenCode" icon="book" href="https://opencode.ai/docs/providers/">
    مرجع المزوّد المخصّص وعنوان URL الأساسي
  </Card>
</CardGroup>
