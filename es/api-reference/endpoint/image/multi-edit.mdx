---
title: 'Edición múltiple de imágenes'
openapi: 'POST /image/multi-edit'
"og:title": "Multi-Edit Image | Venice API Docs"
"og:description": "Edita imágenes utilizando hasta tres entradas en capas (imagen base más máscaras/superposiciones)."
---

<Info>
**Precio:** el precio de la edición múltiple varía según el modelo. Consulta la [vista general de precios](/overview/pricing)
para conocer los precios actuales por edición.
</Info>

## Niveles de calidad

Algunos modelos de edición múltiple aceptan un parámetro opcional `quality` (`low`, `medium`, `high`) que intercambia fidelidad visual por coste. Actualmente lo admite `gpt-image-2-edit`; otros modelos ignoran el parámetro.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

Cuando omites `quality`, el modelo utiliza su nivel predeterminado (`high` para `gpt-image-2-edit`). Los precios por nivel (1K/2K/4K × low/medium/high) están en `model_spec.pricing.quality` en el [endpoint de Models](/api-reference/endpoint/models/list) y se listan en la [vista general de precios](/overview/pricing).

<Info>
La facturación según la calidad es la predeterminada para todos los llamadores de la API y del SDK: no se requiere ninguna cabecera de activación. Las solicitudes que omiten `quality` se facturan al nivel predeterminado del modelo (`high` para `gpt-image-2-edit`).
</Info>
