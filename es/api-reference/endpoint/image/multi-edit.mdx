---
title: 'Multi-edición de imágenes'
openapi: 'POST /image/multi-edit'
"og:title": "Multi-edición de imágenes | Venice API Docs"
"og:description": "Edita imágenes usando hasta tres entradas en capas (imagen base más máscaras/superposiciones)."
---

<Info>
**Precio:** el precio de la multi-edición varía según el modelo. Consulta la [visión general de precios](/es/overview/pricing)
para ver los precios actuales por edición.
</Info>

## Niveles de calidad

Algunos modelos de multi-edición aceptan un parámetro opcional `quality` (`low`, `medium`, `high`) que intercambia fidelidad visual por coste. Actualmente lo admite `gpt-image-2-edit`; otros modelos ignoran el parámetro.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

Si omites `quality`, el modelo usa su nivel predeterminado (`high` para `gpt-image-2-edit`). Los precios por nivel (1K/2K/4K × low/medium/high) viven bajo `model_spec.pricing.quality` en el [endpoint de modelos](/es/api-reference/endpoint/models/list) y se listan en la [visión general de precios](/es/overview/pricing).

<Info>
La facturación según calidad es el comportamiento por defecto para todos los clientes de la API y los SDK — no se requiere ninguna cabecera para activarlo. Las solicitudes que omiten `quality` se cobran al nivel predeterminado del modelo (`high` para `gpt-image-2-edit`).
</Info>
