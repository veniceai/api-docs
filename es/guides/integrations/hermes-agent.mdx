---
title: Hermes Agent
description: Usa Venice AI como tu proveedor de modelos en Hermes Agent
"og:title": "Hermes Agent con Venice | Venice API Docs"
"og:description": "Configura Venice AI como proveedor en Hermes Agent para IA centrada en la privacidad y sin censura con memoria persistente, skills y mensajería multiplataforma."
---

[Hermes Agent](https://hermes-agent.nousresearch.com) es un agente de IA open-source y auto-hospedado creado por [Nous Research](https://nousresearch.com). Cuenta con memoria persistente, creación autónoma de skills y un bucle de aprendizaje integrado que se vuelve más capaz cuanto más tiempo se ejecuta. Apúntalo a la API de Venice y tu agente tendrá acceso a más de 230 modelos y herramientas para texto, imagen, video, audio, embeddings y mucho más.

<Card title="Documentación de Hermes Agent" icon="arrow-up-right-from-square" href="https://hermes-agent.nousresearch.com/docs/">
  Documentación completa, configuración del proveedor y opciones en los docs oficiales de Hermes Agent.
</Card>

## ¿Por qué Venice + Hermes Agent?

La API de Venice da a tu Hermes Agent acceso a toda la plataforma Venice mediante un único endpoint compatible con OpenAI.

| Capacidad | Lo que obtienes |
|---|---|
| **Texto y chat** | Modelos privados y anonimizados (GLM, Qwen, Claude, GPT y más) |
| **Imagen** | Generación, edición, escalado y eliminación de fondo |
| **Video** | Generación y transcripción |
| **Audio** | Síntesis de voz (TTS), generación de música y speech-to-text |
| **Embeddings** | Embeddings vectoriales para RAG y búsqueda semántica |
| **Herramientas** | Web scraping, búsqueda web, parser de texto y RPC cripto |

<CardGroup cols={3}>
  <Card title="Inferencia privada" icon="shield-halved">
    Cero retención de datos. Los prompts nunca se almacenan ni registran
  </Card>
  <Card title="Memoria persistente" icon="brain">
    Hermes recuerda el contexto entre sesiones y reinicios
  </Card>
  <Card title="Más de 15 plataformas" icon="comments">
    Llega a tu agente en Telegram, Discord, Slack, WhatsApp y más
  </Card>
</CardGroup>

## Configuración

### 1. Instala Hermes Agent

<Tabs>
  <Tab title="macOS / Linux">
    ```bash
    curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash
    ```
  </Tab>

  <Tab title="WSL2 (Windows)">
    Instala [WSL2](https://learn.microsoft.com/en-us/windows/wsl/install) primero y luego ejecuta el mismo comando:
    ```bash
    curl -fsSL https://raw.githubusercontent.com/NousResearch/hermes-agent/main/scripts/install.sh | bash
    ```
  </Tab>
</Tabs>

Tras la instalación, recarga tu shell:

```bash
source ~/.zshrc   # o: source ~/.bashrc
```

### 2. Configura Venice como tu proveedor

Ejecuta el asistente de configuración del modelo:

```bash
hermes model
```

Selecciona **Custom endpoint (self-hosted / VLLM / etc.)** e introduce lo siguiente cuando se te pida:

| Campo | Valor |
|---|---|
| **URL base de la API** | `https://api.venice.ai/api/v1` |
| **API key** | Tu API key de Venice |
| **Nombre del modelo** | Un ID de modelo de Venice (ver abajo) |

Obtén una API key en [venice.ai/settings/api](https://venice.ai/settings/api) si aún no tienes.

<Tip>
También puedes configurar Venice directamente en `~/.hermes/config.yaml`:

```yaml
model:
  default: zai-org-glm-5
  provider: custom
  base_url: https://api.venice.ai/api/v1
  api_key: ${VENICE_API_KEY}
```

Y establece la clave en `~/.hermes/.env`:

```bash
VENICE_API_KEY=your-key-here
```
</Tip>

### 3. Elige un modelo

Cuando el asistente te pida un modelo, elige uno según tu caso de uso:

| Caso de uso | Modelo | Privacidad |
|---|---|---|
| General | `zai-org-glm-5` | Privado |
| Razonamiento | `kimi-k2-6` | Privado |
| Programación | `claude-opus-4-7` | Anonimizado |
| Visión | `z-ai-glm-5v-turbo` | Anonimizado |
| Sin censura | `venice-uncensored-1-2` | Privado |

Cambia tu modelo en cualquier momento sin necesidad de reiniciar:

```bash
hermes model              # asistente completo
```

O cambia a mitad de sesión:

```text
/model custom:claude-opus-4-7
```

### 4. Empieza a chatear

Abre la CLI clásica o la TUI moderna:

```bash
hermes            # CLI clásica
hermes --tui      # TUI moderna (recomendada)
```

Prueba un prompt para verificar que todo funciona:

```text
Summarize this repo in 5 bullets and tell me what the main entrypoint is.
```

## Conectar plataformas de mensajería

Una vez funcione la CLI, conecta tus apps de mensajería a través del gateway:

```bash
hermes gateway setup
```

Esto te guía para conectar Telegram, Discord, Slack, WhatsApp, Signal y otras plataformas. Tu agente queda accesible desde cualquier canal conectado, todo impulsado por Venice.

```bash
hermes gateway      # inicia el gateway de mensajería
```

## Modos de privacidad

Los modelos de Venice en Hermes Agent siguen los mismos [niveles de privacidad](/es/overview/privacy) que la API de Venice:

- Los modelos **privados** (GLM, Qwen, DeepSeek, Llama, Venice Uncensored) se ejecutan en la flota de GPUs de Venice. Los prompts nunca se almacenan ni se registran.
- Los modelos **anonimizados** (Claude, GPT, Gemini, Grok) se proxifican a través de Venice eliminando toda información identificativa. El proveedor externo ve a Venice como cliente, no a ti.

## Skills de la API de Venice

Hermes Agent tiene un sistema de skills integrado compatible con el formato [Agent Skills](https://github.com/veniceai/skills). Venice publica skills oficiales que enseñan a tu agente a usar cada endpoint de Venice (chat, generación de imágenes, video, audio, embeddings, herramientas augment y más).

Instala las skills de Venice directamente desde GitHub:

```bash
hermes skills install veniceai/skills
```

O busca skills individuales:

```bash
hermes skills search venice
```

Hermes descubrirá cada skill por su frontmatter `SKILL.md` y la cargará bajo demanda.

## Comandos clave

| Comando | Descripción |
|---|---|
| `hermes` | Empezar a chatear |
| `hermes model` | Cambiar de proveedor o modelo |
| `hermes tools` | Configurar qué herramientas están activadas |
| `hermes gateway` | Iniciar el gateway de mensajería |
| `hermes --continue` | Reanudar la última sesión |
| `hermes doctor` | Diagnosticar problemas |
| `hermes update` | Actualizar a la última versión |

## Recursos

<CardGroup cols={2}>
  <Card title="Documentación de Hermes Agent" icon="book" href="https://hermes-agent.nousresearch.com/docs/">
    Documentación oficial
  </Card>
  <Card title="GitHub" icon="github" href="https://github.com/NousResearch/hermes-agent">
    Código fuente y releases
  </Card>
  <Card title="Catálogo de modelos de Venice" icon="list" href="/es/models/text">
    Explora los modelos disponibles
  </Card>
  <Card title="Privacidad de Venice" icon="shield-halved" href="/es/overview/privacy">
    Cómo Venice protege tus datos
  </Card>
</CardGroup>
