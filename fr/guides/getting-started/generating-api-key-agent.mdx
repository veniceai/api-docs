---
title: Création autonome de clé API par un agent
'og:title': "Clé API d'agent autonome | Documentation Venice API"
'og:description': Comment un agent IA autonome peut émettre sa propre clé API Venice en signant un token émis par Venice avec un portefeuille qui détient du VVV staké sur Base.
---

Un agent IA qui contrôle un portefeuille sur Base peut émettre sa propre clé API Venice sans intervention humaine. L'agent acquiert du VVV, le stake, signe un token de validation à courte durée de vie émis par Venice, puis renvoie le token signé pour recevoir une nouvelle clé API liée au portefeuille de staking.

Ce guide parcourt le flux complet de bout en bout et couvre les options de financement pour effectivement payer l'inférence une fois la clé émise.

## Prérequis

* Un portefeuille EVM sur Base contrôlé par l'agent (clé privée dans une variable d'environnement ou un gestionnaire de secrets).
* Une petite quantité d'ETH sur Base pour le gas (le staking nécessite deux transactions : `approve` puis `stake`).
* N'importe quelle quantité non nulle de VVV à staker. L'endpoint d'émission exige seulement que le portefeuille ait un solde sVVV non nul, donc 1 VVV suffit pour émettre une clé. Voir [Payer l'inférence](#paying-for-inference) pour ce dont vous avez besoin pour réellement appeler les endpoints payants.

<Tip>
  Utilisez un portefeuille dédié à l'agent plutôt qu'un portefeuille de trésorerie. La clé privée du portefeuille signe chaque requête de token Venice, donc son rayon d'impact doit être faible.
</Tip>

## Étapes

<Steps>
  <Step title="Acquérir du VVV">
    Envoyez du VVV au portefeuille de l'agent, ou faites en sorte que l'agent fasse un swap sur un DEX tel que [Aerodrome](https://aerodrome.finance/swap?from=eth\&to=0xacfe6019ed1a7dc6f7b508c02d1b04ec88cc21bf\&chain0=8453\&chain1=8453) ou [Uniswap](https://app.uniswap.org/swap?chain=base\&inputCurrency=NATIVE\&outputCurrency=0xacfe6019ed1a7dc6f7b508c02d1b04ec88cc21bf).

    Contrat du token VVV sur Base : `0xacfE6019Ed1A7Dc6f7B508C02d1b04ec88cC21bf`
  </Step>

  <Step title="Staker VVV avec Venice">
    Stakez le VVV dans le [Smart Contract de staking Venice](https://basescan.org/address/0x321b7ff75154472b18edb199033ff4d116f340ff#code) à l'adresse `0x321b7ff75154472B18EDb199033fF4D116F340Ff`. Il s'agit de deux transactions :

    1. `approve(spender, amount)` sur le token VVV, où `spender` est le contrat de staking.
    2. `stake(amount)` sur le contrat de staking.

    <Frame as="div">
      <img src="/images/guides/SC-Stake.png" alt="Staking dans le smart contract" />
    </Frame>

    Lorsque la deuxième transaction est confirmée, le solde VVV du portefeuille diminue et son solde sVVV augmente du même montant. L'endpoint d'émission lit le solde sVVV pour confirmer que le portefeuille est staké.
  </Step>

  <Step title="Demander un token de validation">
    Appelez `GET /api/v1/api_keys/generate_web3_key` pour obtenir un token à courte durée de vie signé par Venice. L'endpoint n'est pas authentifié.

    ```bash
    curl --request GET \
      --url https://api.venice.ai/api/v1/api_keys/generate_web3_key
    ```

    La réponse contient un champ `token`. Le token expire 15 minutes après son émission, donc signez et soumettez-le bien avant cela.
  </Step>

  <Step title="Signer le token avec le portefeuille de staking">
    Signez la chaîne brute du token avec le portefeuille qui détient le VVV staké. Il s'agit d'un `personal_sign` standard sur les bytes du token. À la fois `ethers.Wallet.signMessage(token)` et `account.signMessage({ message: token })` de `viem` produisent la signature correcte.
  </Step>

  <Step title="Émettre la clé API">
    `POST` l'adresse, la signature et le token vers le même endpoint, avec le type de clé que vous voulez.

    ```bash
    curl --request POST \
      --url https://api.venice.ai/api/v1/api_keys/generate_web3_key \
      --header 'Content-Type: application/json' \
      --data '{
        "address": "<wallet address>",
        "signature": "<signed token>",
        "token": "<unsigned token>",
        "apiKeyType": "INFERENCE",
        "description": "Agent key minted on <date>"
      }'
    ```

    Champs requis : `address`, `signature`, `token`, `apiKeyType` (`INFERENCE` ou `ADMIN`).

    Champs optionnels : `description`, `expiresAt`, `consumptionLimit` (plafonne les dépenses totales sur cette clé, libellé en `usd`, `vcu` ou `diem`).

    En cas de succès, la réponse contient la chaîne `apiKey` émise. Stockez-la dans le gestionnaire de secrets de l'agent et utilisez-la comme un token Bearer normal (`Authorization: Bearer <key>`).
  </Step>
</Steps>

## Exemple de bout en bout

L'exemple ci-dessous utilise un véritable portefeuille issu d'une variable d'environnement plutôt qu'un portefeuille généré aléatoirement. Un portefeuille aléatoire n'a pas de VVV staké et l'émission sera rejetée avec l'erreur `Wallet has no staked VVV on Base`.

```typescript
import { ethers } from "ethers"

const wallet = new ethers.Wallet(process.env.WALLET_PRIVATE_KEY!)
const address = wallet.address

const tokenResponse = await fetch("https://api.venice.ai/api/v1/api_keys/generate_web3_key")
const { data: { token } } = await tokenResponse.json()

const signature = await wallet.signMessage(token)

const mintResponse = await fetch("https://api.venice.ai/api/v1/api_keys/generate_web3_key", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    address,
    signature,
    token,
    apiKeyType: "INFERENCE",
    description: "Agent key",
  }),
})

const result = await mintResponse.json()
if (!mintResponse.ok) {
  throw new Error(`Mint failed: ${result.error}`)
}

console.log("Minted key:", result.data.apiKey)
```

## Référence des erreurs

L'endpoint renvoie des messages d'erreur spécifiques et exploitables. Mappez-les dans l'agent afin qu'il puisse décider de réessayer, de demander un nouveau token ou d'arrêter.

| Statut | Le message d'erreur contient | Ce que cela signifie | Que faire |
| --- | --- | --- | --- |
| `400` | `Invalid wallet address` | Le champ `address` n'est pas une adresse EVM valide. | Corriger l'adresse et resoumettre. |
| `400` | `JWT has expired` | Le token de validation a expiré avant que vous ne le signiez et le soumettiez. | Demander un nouveau token, le signer et soumettre immédiatement. |
| `400` | `JWT signature is invalid` | Le token n'a pas été signé par Venice (probablement falsifié ou fabriqué). | Toujours utiliser un nouveau token issu de l'endpoint `GET`. |
| `400` | `JWT claims are invalid` | L'émetteur ou l'audience du token ne correspond pas à ce que Venice attend. | Utiliser le token non modifié renvoyé par l'endpoint `GET`. |
| `400` | `JWT is malformed` | Le `token` soumis n'est pas un JWT. | Vérifier que vous envoyez la chaîne `token` exacte renvoyée par l'endpoint `GET`. |
| `400` | `Wallet signature does not match` | La `signature` ne correspond pas à l'`address` pour le `token` donné. | Signer les bytes bruts du token avec le portefeuille propriétaire de `address`. |
| `400` | `Could not verify wallet signature` | L'appel RPC pour vérifier la signature a échoué (transitoire). | Réessayer avec backoff. |
| `400` | `Wallet has no staked VVV on Base` | Le portefeuille a un solde sVVV nul. | Stakez du VVV d'abord, puis réessayez. |

## Payer l'inférence

Émettre une clé et pouvoir appeler des endpoints payants avec celle-ci sont deux choses différentes. Une clé fraîchement émise s'authentifie correctement mais ne peut pas appeler les endpoints payants (tels que `/chat/completions`) tant que le compte du portefeuille n'a pas un solde dépensable.

La clé émise peut dépenser depuis le compte utilisateur dans cet ordre de priorité : DIEM, puis crédits groupés, puis USD.

| Source de financement | Autonome ? | Comment |
| --- | --- | --- |
| **DIEM depuis le staking VVV** | Oui | L'allocation DIEM quotidienne du portefeuille est proportionnelle à sa part dans le pool de staking. Le compte a besoin d'au moins 0,1 DIEM staké pour que du DIEM soit dépensable. Des stakes plus importants gagnent proportionnellement plus de DIEM quotidien, rechargé à chaque epoch (00:00 UTC). |
| **USD via Stripe** | Non (navigateur) | Connectez-vous à venice.ai avec le même portefeuille (Sign-In-With-Ethereum). Le tableau de bord trouve l'enregistrement utilisateur existant. Ajoutez des crédits dans Settings, API. |
| **Abonnement crypto Coinbase** | Non (navigateur) | Même connexion par portefeuille, puis abonnez-vous via le tableau de bord. Le flux redirige vers Coinbase Commerce pour le paiement réel, il ne peut donc pas être piloté depuis un script. |
| **Onramp Coinbase** | Non (navigateur) | Même connexion par portefeuille, puis utilisez le widget onramp dans le tableau de bord. Hébergé sur l'interface de Coinbase. |

Si l'agent a besoin d'un chemin de financement entièrement crypto-native et headless, les options les plus propres sont :

1. **Staker plus de VVV** afin que l'allocation DIEM quotidienne couvre les dépenses de l'agent. La clé émise prend cela en compte automatiquement.
2. **Utiliser le [flux de portefeuille x402](/fr/guides/integrations/x402-venice-api) au lieu de la clé API.** Avec x402, l'agent signe un message Sign-In-With-X par requête, recharge directement avec de l'USDC sur Base ou Solana via `POST /api/v1/x402/top-up`, et paie par requête. Le solde USDC x402 est lié au portefeuille, pas à l'utilisateur, il n'apparaît donc pas comme solde pour la clé Bearer émise, mais il permet au même portefeuille de payer l'inférence de manière programmatique.

## Ressources connexes

<CardGroup cols={2}>
  <Card title="Crypto et agents" icon="link" href="/fr/guides/integrations/crypto-rpc-agents">
    Utilisez Venice à la fois comme fournisseur de modèle et comme couche RPC blockchain pour les agents autonomes.
  </Card>

  <Card title="Authentification par portefeuille x402" icon="wallet" href="/fr/guides/integrations/x402-venice-api">
    Payez à la requête avec de l'USDC sur Base ou Solana, sans clé API requise.
  </Card>

  <Card title="Endpoint Generate Web3 API Key" icon="code" href="/fr/api-reference/endpoint/api_keys/generate_web3_key/post">
    Référence de l'endpoint d'émission.
  </Card>

  <Card title="Guide de clé API standard" icon="key" href="/fr/guides/getting-started/generating-api-key">
    Pour les utilisateurs qui préfèrent émettre une clé depuis le tableau de bord.
  </Card>
</CardGroup>
