---
title: Venice Skills
description: Agent Skills officiels pour l'API Venice qui chargent les connaissances Venice dans Claude Code, Cursor, Codex, OpenCode, Hermes et Cline
"og:title": "Venice Skills | Documentation Venice API"
"og:description": "Agent Skills prêts à l'emploi qui apprennent à votre agent de codage à utiliser correctement chaque endpoint Venice."
---

[Venice Skills](https://github.com/veniceai/skills) est la collection canonique d'[Agent Skills](https://www.anthropic.com/news/agent-skills) pour l'API Venice. Chaque skill est un dossier autonome avec un `SKILL.md` qu'un agent LLM charge à la demande pour fonctionner correctement face à une surface spécifique de l'API.

<Card title="GitHub : veniceai/skills" icon="github" href="https://github.com/veniceai/skills">
  19 skills couvrant l'intégralité de l'API Venice. Licence MIT. Maintenus synchronisés avec le [`swagger.yaml`](/fr/api-reference/api-spec) public.
</Card>

<CardGroup cols={3}>
  <Card title="19 Skills" icon="layer-group">
    Un par surface de l'API Venice
  </Card>
  <Card title="Indépendant du runtime" icon="plug">
    Fonctionne avec Claude Code, Cursor, Codex, OpenCode, Hermes, Cline et tout autre hôte Agent Skills
  </Card>
  <Card title="Synchronisé avec la spec" icon="rotate">
    Dérivé de la spec OpenAPI de Venice, avec vérifications CI contre le drift
  </Card>
</CardGroup>

## Pourquoi des skills ?

Sans skills, votre agent doit découvrir les particularités de Venice à la dure : `venice_parameters`, enums de type de modèle, flux 402 payment-required, cycle queue/retrieve vidéo, slugs de personnages, etc. Les skills regroupent ces connaissances dans des fichiers ciblés et chargés à la demande, de sorte que l'agent ne charge que ce dont il a besoin pour la tâche en cours.

Chaque `SKILL.md` inclut :

- Le(s) endpoint(s) qu'il couvre
- Les en-têtes, paramètres et formes de réponse requis
- Un exemple curl et un exemple SDK minimal
- Une section "gotchas" avec les choses sur lesquelles les intégrateurs réels trébuchent

## Catalogue de skills

| Skill | Couvre |
|---|---|
| `venice-api-overview` | URL de base, modes d'auth, en-têtes de réponse, modèle tarifaire, versionnage |
| `venice-auth` | Clés API Bearer + authentification Sign-In-With-X / portefeuille x402 |
| `venice-chat` | `/chat/completions` avec `venice_parameters`, multimodal, outils, raisonnement, streaming |
| `venice-responses` | `/responses`, l'API Responses compatible OpenAI (Alpha) |
| `venice-embeddings` | Modèles `/embeddings`, formats d'encodage, dimensions |
| `venice-image-generate` | `/image/generate`, `/images/generations`, `/image/styles` |
| `venice-image-edit` | `/image/edit`, `/image/multi-edit`, `/image/upscale`, `/image/background-remove` |
| `venice-audio-speech` | Modèles TTS `/audio/speech`, voix, formats, streaming |
| `venice-audio-music` | `/audio/quote`, `/audio/queue`, `/audio/retrieve`, `/audio/complete` |
| `venice-audio-transcription` | `/audio/transcriptions` avec Whisper, Parakeet, Scribe, Wizper, xAI STT |
| `venice-video` | Génération + transcription `/video/*` |
| `venice-models` | `/models`, `/models/traits`, `/models/compatibility_mapping` |
| `venice-characters` | `/characters*` + `venice_parameters.character_slug` |
| `venice-api-keys` | CRUD `/api_keys`, limites de débit, génération de clé Web3 |
| `venice-billing` | `/billing/balance`, `/billing/usage`, `/billing/usage-analytics` |
| `venice-x402` | Crédits de portefeuille `/x402/*`, USDC sur Base ou Solana |
| `venice-crypto-rpc` | Proxy JSON-RPC `/crypto/rpc/*` avec tarification 1×/2×/4× |
| `venice-augment` | `/augment/text-parser`, `/augment/scrape`, `/augment/search` |
| `venice-errors` | Formes d'erreur, 402 payment required, 422 content policy, 429 rate limits, stratégie de retry |

## Installation

Chaque skill est juste un dossier avec un `SKILL.md` qui commence par un frontmatter YAML :

```yaml
---
name: venice-chat
description: When the agent should load this skill and what's in it
---
```

Déposez le dossier `skills/` (ou tout sous-ensemble) dans le chemin que votre runtime surveille.

<Tabs>
  <Tab title="Claude Code">
    Local au projet :

    ```bash
    git clone https://github.com/veniceai/skills.git
    cp -r skills/skills/* .claude/skills/
    ```

    Ou global, pour chaque projet sur votre machine :

    ```bash
    git clone https://github.com/veniceai/skills.git ~/src/venice-skills
    ln -s ~/src/venice-skills/skills ~/.claude/skills/venice
    ```
  </Tab>

  <Tab title="Cursor">
    Local au projet :

    ```bash
    git clone https://github.com/veniceai/skills.git .cursor/skills-venice
    ```

    Ou copiez des skills individuels :

    ```bash
    cp -r skills/venice-chat .cursor/skills/
    ```
  </Tab>

  <Tab title="Codex">
    ```bash
    git clone https://github.com/veniceai/skills.git ~/src/venice-skills
    ln -s ~/src/venice-skills/skills ~/.codex/skills/venice
    ```

    Pour des installs locales au projet, ciblez plutôt `.codex/skills/`.
  </Tab>

  <Tab title="OpenCode">
    ```bash
    git clone https://github.com/veniceai/skills.git ~/src/venice-skills
    ln -s ~/src/venice-skills/skills ~/.config/opencode/skills/venice
    ```

    OpenCode lit aussi `.opencode/skills/`, `.claude/skills/` et `.agents/skills/` depuis la racine du projet.
  </Tab>

  <Tab title="Hermes Agent">
    Hermes dispose d'un installateur intégré pour les skills Venice :

    ```bash
    hermes skills install veniceai/skills
    ```

    Ou symlink directement :

    ```bash
    git clone https://github.com/veniceai/skills.git ~/src/venice-skills
    ln -s ~/src/venice-skills/skills ~/.hermes/skills/venice
    ```
  </Tab>

  <Tab title="Cline">
    ```bash
    git clone https://github.com/veniceai/skills.git .clinerules/skills-venice
    ```
  </Tab>
</Tabs>

### Référence des chemins

| Runtime | Local au projet | Global |
|---|---|---|
| Claude Code | `.claude/skills/` | `~/.claude/skills/` |
| Codex | `.codex/skills/` | `~/.codex/skills/` (ou `$CODEX_HOME/skills/`) |
| OpenCode | `.opencode/skills/` (aussi `.claude/skills/`, `.agents/skills/`) | `~/.config/opencode/skills/` |
| Hermes Agent | `$HERMES_OPTIONAL_SKILLS_DIR` | `~/.hermes/skills/` |
| Cursor | `.cursor/skills/` | `~/.cursor/skills/` |
| Cline | `.clinerules/skills/` | n/a |
| Autres runtimes | `.agents/skills/` (convention) | `~/.agents/skills/` |

<Tip>
Les runtimes qui définissent des champs de frontmatter supplémentaires (`version`, `platforms`, `metadata.*`, `compatibility`, …) sont tenus par la spec d'ignorer les champs inconnus, donc le même fichier de skill fonctionne partout sans forks.
</Tip>

### En tant que submodule git

Si vous voulez des versions épinglées dans votre propre repo :

```bash
git submodule add https://github.com/veniceai/skills.git vendor/venice-skills
```

Puis symlink ou copiez les sous-ensembles voulus dans le chemin de skills de votre agent.

## Comment les agents les chargent

L'agent découvre chaque `SKILL.md` par son `name` et sa `description` dans le frontmatter. Lorsque l'utilisateur demande quelque chose qui correspond à l'objectif d'un skill, l'agent charge ce seul fichier en contexte (pas l'intégralité du catalogue), de sorte que le prompt reste petit et la réponse reste précise.

Par exemple, un agent qui doit générer de la musique chargera `venice-audio-music` et saura immédiatement :

- Que la musique passe par le cycle queue/retrieve/complete, pas par un endpoint synchrone
- Quels modèles sont disponibles et leur tarification par minute
- Comment appeler `/audio/quote` pour l'estimation du coût d'abord
- À quoi devrait ressembler le backoff de polling

Sans le skill, l'agent pourrait essayer d'appeler `/audio/speech` pour de la musique et obtenir une réponse inutile.

## Créer un nouveau skill

1. Copiez `template/` vers `skills/<your-skill>/`.
2. Remplissez le frontmatter et le corps. Gardez `description` concret, puisque c'est ce qu'un agent utilise pour décider quand charger le skill.
3. Liez les skills connexes en bas pour la navigation croisée.
4. Ouvrez une PR contre [`veniceai/skills`](https://github.com/veniceai/skills).

Consultez le `CONTRIBUTING.md` du repo pour les conventions de style (premier paragraphe court, tableaux d'endpoint explicites, exemple curl + un SDK, section "gotchas", ≤ 500 lignes).

## Ressources

<CardGroup cols={2}>
  <Card title="GitHub" icon="github" href="https://github.com/veniceai/skills">
    Code source, guide de contribution et template de skill
  </Card>
  <Card title="Venice MCP Server" icon="plug" href="/fr/guides/integrations/venice-mcp">
    Associez les skills au serveur MCP officiel pour un accès aux outils à l'exécution
  </Card>
  <Card title="Spec Agent Skills" icon="arrow-up-right-from-square" href="https://www.anthropic.com/news/agent-skills">
    Apprenez le format sous-jacent
  </Card>
  <Card title="Spec API Venice" icon="book" href="/fr/api-reference/api-spec">
    La source de vérité OpenAPI dont ces skills sont dérivés
  </Card>
</CardGroup>
