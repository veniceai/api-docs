---
title: "Crypto RPC"
description: "Découvrez les réseaux blockchain pris en charge et envoyez des requêtes JSON-RPC via Venice avec une clé API ou une authentification par portefeuille x402."
"og:title": "Crypto RPC | Venice API Docs"
"og:description": "Accédez aux réseaux blockchain pris en charge via Venice sans gérer un fournisseur RPC distinct."
---

Venice Crypto RPC est un proxy HTTP permettant de lire l'état de la blockchain et de relayer des transactions signées. Il utilise la même clé API Venice ou authentification par portefeuille x402 que les autres points de terminaison payants de Venice, de sorte que votre application n'a pas besoin d'un compte distinct auprès d'un fournisseur RPC.

Venice ne détient jamais vos clés privées. Signez les transactions dans votre application et soumettez la charge utile signée avec `eth_sendRawTransaction`.

## Découvrir les réseaux pris en charge

Récupérez la liste actuelle des identifiants (slugs) de réseaux :

```bash
curl https://api.venice.ai/api/v1/crypto/rpc/networks
```

Le point de terminaison de découverte est public et ne nécessite aucune authentification. Sa réponse fait autorité ; utilisez l'un des slugs renvoyés comme `{network}` dans le point de terminaison RPC.

## Envoyer une requête RPC

Envoyez une requête JSON-RPC 2.0 au réseau sélectionné :

```bash
curl https://api.venice.ai/api/v1/crypto/rpc/ethereum-mainnet \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_blockNumber",
    "params": [],
    "id": 1
  }'
```

Le proxy prend en charge les requêtes uniques et les lots pouvant contenir jusqu'à 100 requêtes. Les méthodes prises en charge, les niveaux de tarification et la disponibilité varient ; consultez la [référence de l'API Crypto RPC](/api-reference/endpoint/crypto/rpc) avant de vous appuyer sur une méthode.

## Flux de lecture et d'écriture

### Lire l'état de la chaîne

Utilisez des méthodes telles que `eth_blockNumber`, `eth_getBalance`, `eth_call` et `eth_getLogs` pour interroger des données publiques de la chaîne. Regroupez les appels associés lorsqu'ils ciblent le même réseau.

### Relayer une transaction

<Steps>
  <Step title="Construire la transaction">
    Lisez le nonce du compte, les données de frais et l'estimation de gaz depuis le réseau cible.
  </Step>
  <Step title="Signer localement">
    Signez avec la bibliothèque de portefeuille de votre application. N'envoyez jamais de clé privée ni de phrase secrète à Venice.
  </Step>
  <Step title="Soumettre la charge utile signée">
    Appelez `eth_sendRawTransaction` et définissez un en-tête `Idempotency-Key` afin que les nouvelles tentatives ne créent pas un second relais facturable.
  </Step>
  <Step title="Vérifier le reçu">
    Interrogez `eth_getTransactionReceipt` jusqu'à ce que la transaction soit incluse, puis inspectez son statut.
  </Step>
</Steps>

## Nouvelles tentatives sûres

Définissez une clé d'idempotence sur les requêtes susceptibles d'être réessayées, en particulier les relais de transactions :

```bash
-H "Idempotency-Key: transaction-123"
```

Venice met en cache la réponse pendant 24 heures. Réutiliser la clé avec le même corps de requête renvoie la réponse mise en cache sans nouvelle facturation ; la réutiliser avec un corps différent renvoie une erreur.

## Contraintes importantes

- Le proxy est uniquement HTTP et ne prend pas en charge les abonnements WebSocket.
- Les méthodes de filtre à état ne sont pas prises en charge ; utilisez des méthodes sans état telles que `eth_getLogs`.
- Venice ne détient pas de clés ni n'expose de méthodes de signature.
- Les méthodes en dehors de la liste d'autorisation renvoient une erreur.
- Inspectez `X-Venice-RPC-Credits` et `X-Venice-RPC-Cost-USD` pour suivre le coût des requêtes.

## Ressources associées

- [API des réseaux pris en charge](/api-reference/endpoint/crypto/networks)
- [Référence de l'API Crypto RPC](/api-reference/endpoint/crypto/rpc)
- [Crypto RPC pour les agents](/guides/integrations/crypto-rpc-agents)
- [Authentification par portefeuille x402](/guides/integrations/x402-venice-api)
