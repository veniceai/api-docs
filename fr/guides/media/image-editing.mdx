---
title: "Édition d'image"
description: "Éditez, faites de l'inpainting, composez et supprimez les arrière-plans des images à l'aide des API d'image synchrones de Venice"
'og:title': "Édition d'image | Documentation Venice API"
'og:description': "Apprenez à éditer des images avec Venice en utilisant l'inpainting par prompt, des entrées multi-edit en couches et la suppression d'arrière-plan."
---

L'édition d'image sur Venice est synchrone. Envoyez votre image source à `/image/edit` ou `/image/multi-edit` et le résultat édité revient dans la même réponse sous forme de fichier PNG. Pour les découpages, `/image/background-remove` renvoie un PNG transparent.

<Warning>
Les endpoints d'édition d'image sont expérimentaux et le comportement spécifique au modèle peut évoluer au fil du temps.
</Warning>

## Endpoints

| Endpoint | Objectif | Idéal pour |
|---|---|---|
| `POST /image/edit` | Éditer une seule image avec un prompt | Éditions générales et inpainting piloté par prompt |
| `POST /image/multi-edit` | Éditer en utilisant 1 à 3 images en couches | Éditions plus contrôlées avec masques ou superpositions |
| `POST /image/background-remove` | Supprimer l'arrière-plan d'une image | Découpages transparents pour produits, portraits et assets |

## Quel endpoint utiliser

- Utilisez `/image/edit` lorsque vous avez une image source et que vous voulez en modifier, supprimer ou restyler une partie avec un prompt.
- Utilisez `/image/multi-edit` lorsque vous avez besoin de contrôle supplémentaire via des masques, superpositions ou couches de référence.
- Utilisez `/image/background-remove` lorsque vous voulez uniquement un sujet de premier plan propre avec de la transparence.

<Note>
Pour l'inpainting, utilisez `/image/edit` ou `/image/multi-edit`. L'ancien paramètre `inpaint` sur `/image/generate` est déprécié.
</Note>

## Étape 1 : éditer une seule image

L'édition d'image unique est le flux d'inpainting le plus simple. Envoyez une image plus un prompt court tel que "remove the sign", "change the sky to sunrise" ou "replace the background with a studio backdrop".

**Requête :**
```bash
POST https://api.venice.ai/api/v1/image/edit
Authorization: Bearer $VENICE_API_KEY
Content-Type: application/json

{
  "model": "qwen-edit",
  "prompt": "Replace the cloudy sky with a warm sunrise while preserving the buildings and canal",
  "image": "https://example.com/venice-canal.jpg"
}
```

**Réponse (200) :**
Le corps de la réponse est des données binaires `image/png` brutes. Enregistrez-le directement dans un fichier.

<CodeGroup>
```python Python
import base64
import os
import requests

with open("input.jpg", "rb") as f:
    image_base64 = base64.b64encode(f.read()).decode("utf-8")

response = requests.post(
    "https://api.venice.ai/api/v1/image/edit",
    headers={
        "Authorization": f"Bearer {os.environ['VENICE_API_KEY']}",
        "Content-Type": "application/json",
    },
    json={
        "model": "qwen-edit",
        "prompt": "Remove the tourist crowd from the square and keep the architecture intact",
        "image": image_base64,
    },
)

with open("edited.png", "wb") as f:
    f.write(response.content)
```

```javascript Node.js
import fs from "fs";

const imageBase64 = fs.readFileSync("input.jpg").toString("base64");

const response = await fetch("https://api.venice.ai/api/v1/image/edit", {
  method: "POST",
  headers: {
    Authorization: `Bearer ${process.env.VENICE_API_KEY}`,
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    model: "qwen-edit",
    prompt: "Remove the tourist crowd from the square and keep the architecture intact",
    image: imageBase64,
  }),
});

const editedImage = Buffer.from(await response.arrayBuffer());
fs.writeFileSync("edited.png", editedImage);
```

```bash cURL
curl https://api.venice.ai/api/v1/image/edit \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -o edited.png \
  -d '{
    "model": "qwen-edit",
    "prompt": "Colorize this black and white portrait naturally",
    "image": "https://example.com/portrait-bw.jpg"
  }'
```
</CodeGroup>

## Étape 2 : utiliser multi-edit pour des masques ou inpainting en couches

`/image/multi-edit` accepte jusqu'à trois images. La première image est l'image de base. Les images restantes sont traitées comme des couches d'édition ou masques, ce qui vous donne plus de contrôle que l'édition uniquement par prompt.

C'est le meilleur choix lorsque vous voulez :

- cibler une région spécifique avec un masque
- combiner une composition existante avec une superposition
- contraindre l'édition plus étroitement qu'un prompt d'image unique ne le peut

**Requête JSON :**
```json
{
  "modelId": "qwen-edit",
  "prompt": "Replace the blank billboard area with a glowing Venice film festival poster while preserving lighting and perspective",
  "images": [
    "https://example.com/street-scene.png",
    "https://example.com/billboard-mask.png"
  ]
}
```

**Requête multipart :**
```bash
curl https://api.venice.ai/api/v1/image/multi-edit \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -F "modelId=qwen-edit" \
  -F "prompt=Replace the blank billboard area with a glowing Venice film festival poster while preserving lighting and perspective" \
  -F "images=@street-scene.png" \
  -F "images=@billboard-mask.png" \
  -o multi-edited.png
```

Comme `/image/edit`, le corps de la réponse est des données `image/png` brutes.

<Note>
`/image/multi-edit` utilise actuellement le champ `modelId` plutôt que `model` dans le schéma de requête.
</Note>

---

## Conseils d'inpainting

L'inpainting basé sur les prompts fonctionne mieux lorsque l'instruction est courte et locale :

- `remove the tree`
- `change the sky to sunset`
- `replace the logo with a blank sign`
- `restore the torn corner of the photo`

Pour des changements de scène plus larges, décrivez ce qui doit rester pareil :

```text
Replace the background with a modern photo studio backdrop while preserving the subject pose, facial features, and clothing.
```

Si l'édition continue d'affecter la mauvaise zone, passez de `/image/edit` à `/image/multi-edit` et fournissez une couche de masque ou de superposition.

---

## Étape 3 : supprimer l'arrière-plan

Utilisez `/image/background-remove` lorsque vous voulez le sujet de premier plan isolé sur un arrière-plan transparent. Cet endpoint renvoie un PNG avec transparence alpha.

**Avec une URL d'image :**
```bash
curl https://api.venice.ai/api/v1/image/background-remove \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -o cutout.png \
  -d '{
    "image_url": "https://example.com/product-photo.jpg"
  }'
```

**Avec un upload de fichier local :**
```bash
curl https://api.venice.ai/api/v1/image/background-remove \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -F "image=@product-photo.jpg" \
  -o cutout.png
```

Utilisez la suppression d'arrière-plan pour :

- les photos produit e-commerce
- les photos de profil et portraits
- les assets que vous prévoyez de placer sur un nouveau fond

---

## Paramètres de requête

### `/image/edit`

| Paramètre | Type | Requis | Défaut | Description |
|---|---|---|---|---|
| `image` | fichier, chaîne base64 ou URL | Oui | - | Image source à éditer |
| `prompt` | string | Oui | - | Instructions textuelles pour l'édition |
| `model` | string | Non | `qwen-edit` | ID du modèle d'édition |
| `aspect_ratio` | string | Non | défaut du modèle | Ratio de sortie pour les modèles qui le prennent en charge |
| `modelId` | string | Déprécié | - | Alias déprécié pour `model` |

### `/image/multi-edit`

| Paramètre | Type | Requis | Défaut | Description |
|---|---|---|---|---|
| `images` | tableau de 1 à 3 fichiers, chaînes base64 ou URLs | Oui | - | La première image est l'image de base ; le reste sont des couches d'édition ou masques |
| `prompt` | string | Oui | - | Instructions textuelles pour combiner ou éditer les couches |
| `modelId` | string | Non | `qwen-edit` | ID du modèle d'édition |

### `/image/background-remove`

| Paramètre | Type | Requis | Description |
|---|---|---|---|
| `image` | fichier ou chaîne base64 | L'un de `image` ou `image_url` | Image source à découper |
| `image_url` | string | L'un de `image` ou `image_url` | URL d'image publique à découper |

---

## Formats d'entrée pris en charge

| Endpoint | Entrée JSON | Entrée multipart | Sortie |
|---|---|---|---|
| `/image/edit` | Chaîne base64 ou URL | Upload de fichier | `image/png` |
| `/image/multi-edit` | Chaînes base64 ou URLs | Uploads de fichiers | `image/png` |
| `/image/background-remove` | Chaîne base64 ou URL | Upload de fichier | `image/png` |

Pour les endpoints d'édition, les dimensions d'image doivent être d'au moins `65536` pixels et au plus `33177600` pixels. Les fichiers téléversés doivent faire moins de `25 Mo`.

---

## Modèles et tarification

Le modèle d'édition par défaut est `qwen-edit`, au prix de **0,04 $ par édition**. D'autres modèles capables d'édition peuvent avoir des tarifs et contraintes différents.

Voir :

- [Tarification image](/fr/overview/pricing)
- [API Models](/fr/api-reference/endpoint/models/list) avec `type=inpaint`

---

## Erreurs

| Statut | Signification | Action |
|---|---|---|
| `400` | Paramètres de requête invalides | Vérifiez le nombre d'images, les noms de champs et le format d'entrée |
| `401` | Authentification échouée | Vérifiez votre clé API |
| `402` | Solde insuffisant | Ajoutez des crédits sur [venice.ai/settings/api](https://venice.ai/settings/api) |
| `415` | Type de contenu invalide | Utilisez JSON ou multipart form-data correctement |
| `429` | Limite de débit dépassée ou modèle surchargé | Réessayez avec backoff ; vérifiez l'en-tête `Retry-After` |
| `500` | Traitement d'inférence échoué | Réessayez la requête |
| `503` | Modèle à capacité | Réessayez après un court délai |

<Note>
Certains modèles d'édition ont des politiques de contenu plus strictes que les modèles de génération d'image. Par exemple, `qwen-edit` bloque les requêtes impliquant des images à caractère sexuel explicite, des mineurs sexualisés ou de la violence du monde réel.
</Note>

---

## Workflows connexes

- Utilisez [Génération d'image](/fr/guides/media/image-generation) lorsque vous partez d'un texte plutôt que d'une image existante.
- Utilisez [Modèles d'image](/fr/models/image) pour comparer les familles de modèles de génération, d'édition et d'amélioration.
- Utilisez [API Image Edit](/fr/api-reference/endpoint/image/edit), [API Multi-Edit](/fr/api-reference/endpoint/image/multi-edit) et [API Background Remove](/fr/api-reference/endpoint/image/background-remove) pour les détails complets du schéma.
