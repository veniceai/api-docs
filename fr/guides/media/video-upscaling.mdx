---
title: "Upscaling vidéo"
description: "Améliorez la résolution et la qualité des vidéos avec le modèle Topaz Video Upscale via l'API Venice"
'og:title': "Guide d'upscaling vidéo | Documentation Venice API"
'og:description': "Améliorez la résolution et la qualité des vidéos avec le modèle Topaz Video Upscale via l'API Venice"
---

L'upscaling vidéo vous permet d'améliorer des vidéos existantes vers des résolutions plus élevées tout en améliorant la qualité visuelle. Le modèle **Topaz Video Upscale** utilise un upscaling assisté par IA pour augmenter la résolution par 2x ou 4x, ou appliquer une amélioration de qualité à la résolution d'origine (1x).

## Comment ça marche

L'upscaling vidéo utilise le même système de file d'attente asynchrone que la génération vidéo :

1. **Queue** — Soumettez votre vidéo à `/video/queue` avec le modèle `topaz-video-upscale`
2. **Poll** — Vérifiez `/video/retrieve` avec le `queue_id` renvoyé jusqu'à ce que le statut soit `completed`
3. **Complete** — Appelez `/video/complete` pour finaliser et obtenir l'URL de sortie

Le serveur détecte automatiquement la durée, la fréquence d'images et les dimensions de la vidéo d'entrée à partir du fichier téléversé. Vous n'avez pas besoin de fournir ces valeurs — la facturation est calculée à partir des métadonnées réelles de la vidéo.

## Facteurs d'upscale

| `upscale_factor` | Résolution de sortie | Cas d'usage |
|---|---|---|
| `1` | Identique à l'entrée | Amélioration de qualité uniquement (débruitage, netteté) |
| `2` (défaut) | 2x les dimensions d'entrée | Upscale standard — une entrée 720p devient 1440p en sortie |
| `4` | 4x les dimensions d'entrée | Upscale maximum — une entrée 480p devient 1920p en sortie |

<Note>
Le paramètre `upscale_factor` remplace `resolution` pour les modèles d'upscale. Passer `resolution` renverra une erreur. C'est parce que la résolution de sortie dépend des dimensions de la vidéo d'entrée — un upscale `2x` d'une vidéo 720p produit un résultat différent d'un upscale `2x` d'une vidéo 480p.
</Note>

## Formats d'entrée pris en charge

- **Formats** : MP4, MOV, WebM
- **Méthodes d'entrée** : URL HTTPS ou data URL `data:video/...;base64,...`
- **Durée max** : 300 secondes (5 minutes)

## Utilisation de l'API

### Mettre en file un job d'upscale

<CodeGroup>
```bash cURL
curl https://api.venice.ai/api/v1/video/queue \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "topaz-video-upscale",
    "video_url": "https://example.com/input-video.mp4",
    "upscale_factor": 2
  }'
```

```python Python
import requests

response = requests.post(
    "https://api.venice.ai/api/v1/video/queue",
    headers={"Authorization": "Bearer YOUR_API_KEY"},
    json={
        "model": "topaz-video-upscale",
        "video_url": "https://example.com/input-video.mp4",
        "upscale_factor": 2,
    },
)

data = response.json()
queue_id = data["queue_id"]
print(f"Queued: {queue_id}")
```

```javascript Node.js
const response = await fetch("https://api.venice.ai/api/v1/video/queue", {
  method: "POST",
  headers: {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json",
  },
  body: JSON.stringify({
    model: "topaz-video-upscale",
    video_url: "https://example.com/input-video.mp4",
    upscale_factor: 2,
  }),
});

const { queue_id } = await response.json();
console.log(`Queued: ${queue_id}`);
```
</CodeGroup>

La réponse inclut un `queue_id` pour suivre le job :

```json
{
  "model": "topaz-video-upscale",
  "queue_id": "abc123-def456-..."
}
```

### Sonder pour la complétion

<CodeGroup>
```bash cURL
curl https://api.venice.ai/api/v1/video/retrieve \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"queue_id": "abc123-def456-..."}'
```

```python Python
import time

while True:
    result = requests.post(
        "https://api.venice.ai/api/v1/video/retrieve",
        headers={"Authorization": "Bearer YOUR_API_KEY"},
        json={"queue_id": queue_id},
    )
    data = result.json()

    if data.get("status") == "completed":
        print(f"Video URL: {data['url']}")
        break

    time.sleep(5)
```

```javascript Node.js
const poll = async (queueId) => {
  while (true) {
    const res = await fetch("https://api.venice.ai/api/v1/video/retrieve", {
      method: "POST",
      headers: {
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json",
      },
      body: JSON.stringify({ queue_id: queueId }),
    });
    const data = await res.json();

    if (data.status === "completed") {
      console.log(`Video URL: ${data.url}`);
      return data;
    }

    await new Promise((r) => setTimeout(r, 5000));
  }
};

await poll(queue_id);
```
</CodeGroup>

### Finaliser avec complete

Après avoir récupéré le résultat, appelez `/video/complete` pour finaliser :

```bash
curl https://api.venice.ai/api/v1/video/complete \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"queue_id": "abc123-def456-..."}'
```

---

## Paramètres de l'API

| Champ | Type | Requis | Description |
|---|---|---|---|
| `model` | string | **Oui** | Doit être `topaz-video-upscale` |
| `video_url` | string | **Oui** | URL ou data URL de la vidéo d'entrée. Formats pris en charge : MP4, MOV, WebM. |
| `upscale_factor` | number | Non | `1`, `2` (défaut) ou `4`. Contrôle le multiplicateur d'upscale. |

### Paramètres non utilisés pour les modèles d'upscale

Les paramètres suivants **ne sont pas acceptés** pour `topaz-video-upscale` et renverront une erreur s'ils sont fournis :

| Champ | Raison |
|---|---|
| `resolution` | Utilisez `upscale_factor` à la place. La résolution de sortie dépend des dimensions d'entrée. |
| `prompt` | L'upscaling n'utilise pas de prompts textuels. Une chaîne vide est définie automatiquement. |

Le paramètre `duration` est aussi ignoré — le serveur détecte la durée directement depuis le fichier vidéo pour la précision de la facturation.

---

## Tarification

La tarification est basée sur la **durée**, le **palier de résolution de sortie** et la **fréquence d'images**. Le palier de résolution de sortie est déterminé par la hauteur de la vidéo d'entrée multipliée par le facteur d'upscale.

### Paliers de résolution de sortie

| Palier | Hauteur de sortie | Tarif par seconde |
|---|---|---|
| 720p | ≤ 720 px | ~0,013 $ |
| 1080p | 721–1080 px | ~0,025 $ |
| 4K | > 1080 px | ~0,10 $ |

<Note>
Les vidéos avec des fréquences d'images supérieures à 48 fps coûtent 2x le tarif par seconde.
</Note>

### Exemples de tarification

| Entrée | Facteur d'upscale | Sortie | Durée | Coût estimé |
|---|---|---|---|---|
| 480p, 30 fps | 2x | 960p (palier 1080p) | 10 s | ~0,25 $ |
| 720p, 30 fps | 2x | 1440p (palier 4K) | 10 s | ~1,00 $ |
| 1080p, 30 fps | 2x | 2160p (palier 4K) | 30 s | ~3,00 $ |
| 360p, 24 fps | 4x | 1440p (palier 4K) | 10 s | ~1,00 $ |
| 480p, 60 fps | 2x | 960p (palier 1080p) | 10 s | ~0,50 $ |

Utilisez l'[API Video Quote](/fr/api-reference/endpoint/video/quote) pour obtenir une tarification exacte avant de soumettre un job.

### Obtenir un devis

```bash
curl https://api.venice.ai/api/v1/video/quote \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "topaz-video-upscale",
    "duration": "10",
    "input_height": 720
  }'
```

L'endpoint quote accepte `input_height` afin de pouvoir estimer le palier de résolution de sortie. C'est optionnel — s'il est omis, le devis suppose une estimation conservatrice.

---

## Dépannage

| Problème | Cause probable | Correction |
|---|---|---|
| `"Use upscale_factor instead of resolution"` | `resolution` a été passé dans la requête | Retirez `resolution` et utilisez `upscale_factor` à la place |
| Coût plus élevé que prévu | La vidéo d'entrée a une résolution élevée ou un FPS élevé | Vérifiez les dimensions d'entrée avec l'endpoint quote. Une entrée 720p+ avec upscale 2x se retrouve dans le palier tarifaire 4K. |
| Le job prend longtemps | Vidéo volumineuse ou longue | L'upscaling est intensif en calcul. Les vidéos plus longues et les facteurs d'upscale plus élevés prennent proportionnellement plus de temps. |
| `"Insufficient balance"` | Crédits du compte trop bas | Ajoutez des crédits sur [venice.ai/settings/api](https://venice.ai/settings/api) |
