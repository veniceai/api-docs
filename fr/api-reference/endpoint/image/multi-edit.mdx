---
title: 'Édition multi-images'
openapi: 'POST /image/multi-edit'
"og:title": "Édition multi-images | Venice API Docs"
"og:description": "Éditez des images en utilisant jusqu'à trois entrées superposées (image de base plus masques/superpositions)."
---

<Info>
**Tarification :** Le prix de l'édition multi-images varie selon le modèle. Voir l'[aperçu de la tarification](/overview/pricing)
pour les prix actuels par édition.
</Info>

## Niveaux de qualité

Certains modèles d'édition multi-images acceptent un paramètre `quality` optionnel (`low`, `medium`, `high`) qui arbitre entre fidélité visuelle et coût. Actuellement pris en charge par `gpt-image-2-edit` ; les autres modèles ignorent ce paramètre.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

Lorsque vous omettez `quality`, le modèle utilise son niveau par défaut (`high` pour `gpt-image-2-edit`). Les prix par niveau (1K/2K/4K × low/medium/high) se trouvent sous `model_spec.pricing.quality` dans l'[endpoint Models](/api-reference/endpoint/models/list) et sont listés dans l'[aperçu de la tarification](/overview/pricing).

<Info>
La tarification consciente de la qualité est la valeur par défaut pour tous les appelants de l'API et du SDK — aucun en-tête d'opt-in requis. Les requêtes qui omettent `quality` sont facturées au niveau par défaut du modèle (`high` pour `gpt-image-2-edit`).
</Info>
