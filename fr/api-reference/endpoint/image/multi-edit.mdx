---
title: 'Édition multi-image'
openapi: 'POST /image/multi-edit'
"og:title": "Édition multi-image | Documentation Venice API"
"og:description": "Modifiez des images en utilisant jusqu'à trois entrées superposées (image de base plus masques/superpositions)."
---

<Info>
**Tarification :** la tarification multi-édition varie selon le modèle. Consultez l'[aperçu de la tarification](/fr/overview/pricing)
pour les prix actuels par édition.
</Info>

## Paliers de qualité

Certains modèles multi-édition acceptent un paramètre optionnel `quality` (`low`, `medium`, `high`) qui arbitre entre fidélité visuelle et coût. Actuellement pris en charge par `gpt-image-2-edit` ; les autres modèles ignorent ce paramètre.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

Lorsque vous omettez `quality`, le modèle utilise son palier par défaut (`high` pour `gpt-image-2-edit`). Les prix par palier (1K/2K/4K × low/medium/high) se trouvent sous `model_spec.pricing.quality` dans l'[endpoint Models](/fr/api-reference/endpoint/models/list) et sont listés dans l'[aperçu de la tarification](/fr/overview/pricing).

<Info>
La tarification tenant compte de la qualité est la valeur par défaut pour tous les appelants API et SDK — aucun en-tête d'opt-in requis. Les requêtes qui omettent `quality` sont facturées au palier par défaut du modèle (`high` pour `gpt-image-2-edit`).
</Info>
