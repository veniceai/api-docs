---
title: 'Lister les avis sur un personnage'
openapi: 'GET /characters/{slug}/reviews'
"og:title": "Lister les avis sur un personnage | Documentation Venice API"
"og:description": "Documentation sur l'API d'avis sur les personnages de Venice."
---

## Endpoint expérimental

<Warning>
Il s'agit d'un endpoint expérimental susceptible d'évoluer.
</Warning>

## Ce que renvoie cet endpoint

Cet endpoint renvoie les avis publics paginés pour un personnage donné.

- Utilisez le paramètre de chemin `slug` pour identifier le personnage.
- Utilisez les paramètres de requête `page` et `pageSize` pour paginer les avis.
- Les métadonnées de pagination sont renvoyées à la fois dans le corps de la réponse et dans les en-têtes de réponse `x-pagination-*`.
