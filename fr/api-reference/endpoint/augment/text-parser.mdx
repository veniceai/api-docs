---
title: 'Analyseur de texte'
openapi: 'POST /augment/text-parser'
"og:title": "Analyseur de texte | Documentation Venice API"
"og:description": "Documentation sur l'API d'analyseur de texte de Venice pour extraire du texte depuis des fichiers PDF, DOCX, XLSX et texte brut."
---

<Warning>Il s'agit d'une API expérimentale. Le format des requêtes et des réponses peut évoluer sans préavis.</Warning>

Téléversez un fichier de document via multipart/form-data en utilisant le champ `file`. Les formats pris en charge comprennent les fichiers **PDF**, **DOCX**, **XLSX** et **texte brut** (jusqu'à 25 Mo).

Définissez `response_format` sur `json` (par défaut) pour une sortie structurée avec le texte extrait et le nombre de tokens, ou `text` pour le texte extrait brut.

**Confidentialité :** L'analyse de texte s'exécute entièrement en mémoire sur l'infrastructure de Venice avec zéro rétention de données. Vos documents sont traités puis immédiatement supprimés — aucun contenu n'est stocké ni journalisé.

**Tarification :** 0,01 $ par requête.

### Exemple (cURL)

```bash
curl -X POST https://api.venice.ai/api/v1/augment/text-parser \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -F "file=@document.pdf" \
  -F "response_format=json"
```

-------
