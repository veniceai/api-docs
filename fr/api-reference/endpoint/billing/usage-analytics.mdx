---
title: 'Analyses d''utilisation de facturation (Bêta)'
openapi: 'GET /billing/usage-analytics'
"og:title": "Analyses d'utilisation de facturation | Documentation Venice API"
"og:description": "Obtenez des analyses d'utilisation agrégées avec des ventilations par date, modèle et clé API."
---

<Warning>
Il s'agit d'un endpoint bêta qui peut être instable ou évoluer sans préavis.
</Warning>

Obtenez des analyses d'utilisation agrégées pour l'utilisateur authentifié, avec des ventilations par date, modèle et clé API. Cet endpoint fournit des vues récapitulatives de vos données d'utilisation de l'API pour construire des tableaux de bord et surveiller la consommation. Les données sont mises en cache pendant 10 minutes.

## Paramètres de requête

Vous pouvez spécifier la période d'analyse en utilisant soit :
- **lookback** : Une période relative comme "7d" (7 jours), "30d" (30 jours), jusqu'à "90d" (90 jours)
- **startDate** et **endDate** : Une plage de dates personnalisée au format `YYYY-MM-DD`. Les deux sont obligatoires si l'un est fourni.

Si aucun paramètre n'est spécifié, la période lookback par défaut est de 7 jours.

## Champs de réponse

### lookback
La période lookback utilisée pour la requête. Soit au format "Nd" (par ex. "7d"), soit au format "startDate:endDate".

### byDate
Totaux d'utilisation quotidiens pour la période demandée.

- **date** : La date au format `YYYY-MM-DD`
- **USD** : Utilisation totale en USD pour ce jour
- **DIEM** : Utilisation totale en DIEM pour ce jour

### byModel
Ventilation de l'utilisation par modèle, triée par dépense totale (la plus élevée d'abord).

- **modelName** : Nom d'affichage du modèle (par ex. "GLM 5")
- **unitType** : Type d'unités consommées (tokens, images, chars, minutes, seconds)
- **modelType** : Type de modèle (LLM, IMAGE, TTS, ASR, VIDEO), ou null
- **totalUsd** : USD total dépensé sur ce modèle
- **totalDiem** : DIEM total dépensé sur ce modèle
- **totalUnits** : Total des unités consommées pour ce modèle
- **breakdown** : Tableau des ventilations d'utilisation par type (présent uniquement s'il y a plusieurs types). Chaque entrée contient :
  - **type** : Type de token (par ex. "Input", "Output", "Cache Read", "Cache Write")
  - **usd** : Montant en USD pour cette ventilation
  - **diem** : Montant en DIEM pour cette ventilation
  - **units** : Nombre d'unités pour cette ventilation

### byModelDaily
Données quotidiennes pour les graphiques des 8 principaux modèles. Chaque entrée contient une "date" (timestamp) plus les noms de modèles comme clés avec les valeurs d'utilisation en DIEM.

### topModels
Tableau des 8 principaux noms de modèles par utilisation, pour les légendes de graphiques.

### byKey
Ventilation de l'utilisation par clé API, triée par dépense totale (la plus élevée d'abord).

- **apiKeyId** : L'identifiant de la clé API, ou null si l'utilisation provenait de l'application web
- **description** : Description de la clé API ou "Web App"
- **totalUsd** : USD total dépensé via cette clé
- **totalDiem** : DIEM total dépensé via cette clé
- **totalUnits** : Total des unités consommées via cette clé

### byKeyDaily
Données quotidiennes pour les graphiques des 8 principales clés API. Chaque entrée contient une "date" (timestamp) plus les descriptions des clés comme clés avec les valeurs d'utilisation en DIEM.

### topKeyNames
Tableau des 8 principales descriptions de clés API par utilisation, pour les légendes de graphiques.

## Exemple d'utilisation

```bash
# Obtenir les analyses d'utilisation pour les 7 derniers jours (par défaut)
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics" \
  -H "Authorization: Bearer YOUR_API_KEY"

# Obtenir les analyses d'utilisation pour les 30 derniers jours
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics?lookback=30d" \
  -H "Authorization: Bearer YOUR_API_KEY"

# Obtenir les analyses d'utilisation pour une plage de dates spécifique
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics?startDate=2024-01-01&endDate=2024-01-31" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
