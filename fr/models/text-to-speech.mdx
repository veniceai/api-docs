---
title: "Modèles Text-to-Speech"
description: "Modèles de synthèse vocale avec prise en charge de voix multilingues"
"og:title": "Modèles Text-to-Speech | Documentation Venice API"
---

<div id="model-search-placeholder" data-filter="tts">Loading models...</div>

---

## Catalogue de voix

Les voix sont **spécifiques au modèle**. La `voice` que vous transmettez doit provenir du catalogue
du `model` que vous avez sélectionné. Choisissez un modèle ci-dessous pour parcourir ses voix.

<div id="tts-voice-picker-placeholder">Loading voices...</div>

<Note>
Les identifiants de voix sont sensibles à la casse et **uniquement valides pour le `model` correspondant**. Transmettez
les deux champs ensemble dans la charge utile de votre requête. Consultez l'
[API Audio Speech](/fr/api-reference/endpoint/audio/speech) pour des exemples.
</Note>

### Exemple de requête

```bash
curl https://api.venice.ai/api/v1/audio/speech \
  -H "Authorization: Bearer $VENICE_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "tts-kokoro",
    "voice": "af_sky",
    "input": "Hello from Venice."
  }' \
  --output speech.mp3
```

Pour changer de modèle, modifiez **à la fois** `model` et `voice` pour une paire issue du
modèle sélectionné ci-dessus.
