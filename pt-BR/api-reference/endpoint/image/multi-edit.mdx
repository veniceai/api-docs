---
title: 'Edição múltipla de imagem'
openapi: 'POST /image/multi-edit'
"og:title": "Edição múltipla de imagem | Documentação da API Venice"
"og:description": "Edite imagens usando até três entradas em camadas (imagem base mais máscaras/sobreposições)."
---

<Info>
**Preço:** O preço de edição múltipla varia por modelo. Veja a [visão geral de preços](/overview/pricing)
para os preços atuais por edição.
</Info>

## Níveis de qualidade

Alguns modelos de edição múltipla aceitam um parâmetro opcional `quality` (`low`, `medium`, `high`) que troca fidelidade visual por custo. Atualmente suportado por `gpt-image-2-edit`; outros modelos ignoram o parâmetro.

```json
{
  "modelId": "gpt-image-2-edit",
  "images": ["iVBORw0KGgo...", "iVBORw0KGgo..."],
  "prompt": "blend the two scenes into one cinematic frame",
  "resolution": "2K",
  "quality": "medium"
}
```

Quando você omite `quality`, o modelo usa seu nível padrão (`high` para `gpt-image-2-edit`). Os preços por nível (1K/2K/4K × low/medium/high) ficam em `model_spec.pricing.quality` no [endpoint de Modelos](/api-reference/endpoint/models/list) e estão listados na [visão geral de preços](/overview/pricing).

<Info>
Preços baseados em qualidade são o padrão para todas as chamadas de API e SDK — não é necessário cabeçalho de opt-in. Requisições que omitem `quality` são cobradas pelo nível padrão do modelo (`high` para `gpt-image-2-edit`).
</Info>

