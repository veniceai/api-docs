---
title: 'Analytics de uso de cobrança (Beta)'
openapi: 'GET /billing/usage-analytics'
"og:title": "Analytics de uso de cobrança | Documentação da API Venice"
"og:description": "Obtenha analytics de uso agregadas com detalhamento por data, modelo e chave de API."
---

<Warning>
Este é um endpoint em beta e pode ser instável ou sofrer mudanças sem aviso prévio.
</Warning>

Obtenha analytics agregadas de uso para o usuário autenticado, com detalhamento por data, modelo e chave de API. Este endpoint fornece visões de resumo dos dados de uso da sua API para construir dashboards e monitorar o consumo. Os dados são cacheados por 10 minutos.

## Parâmetros de consulta

Você pode especificar o período de tempo das analytics usando:
- **lookback**: Um período relativo como "7d" (7 dias), "30d" (30 dias), até "90d" (90 dias)
- **startDate** e **endDate**: Um intervalo de datas personalizado no formato `YYYY-MM-DD`. Ambos são obrigatórios caso algum seja fornecido.

Se nenhum parâmetro for especificado, o período de lookback padrão é de 7 dias.

## Campos de resposta

### lookback
O período de lookback usado para a consulta. No formato "Nd" (por exemplo, "7d") ou no formato "startDate:endDate".

### byDate
Totais diários de uso para o período solicitado.

- **date**: A data no formato `YYYY-MM-DD`
- **USD**: Uso total em USD para aquele dia
- **DIEM**: Uso total em DIEM para aquele dia

### byModel
Detalhamento de uso por modelo, ordenado por gasto total (maior primeiro).

- **modelName**: Nome de exibição do modelo (por exemplo, "GLM 5")
- **unitType**: Tipo de unidades consumidas (tokens, images, chars, minutes, seconds)
- **modelType**: Tipo de modelo (LLM, IMAGE, TTS, ASR, VIDEO) ou null
- **totalUsd**: Total em USD gasto neste modelo
- **totalDiem**: Total em DIEM gasto neste modelo
- **totalUnits**: Total de unidades consumidas para este modelo
- **breakdown**: Array de detalhamentos de uso por tipo (presente apenas se houver múltiplos tipos). Cada entrada contém:
  - **type**: Tipo de token (por exemplo, "Input", "Output", "Cache Read", "Cache Write")
  - **usd**: Valor em USD para este detalhamento
  - **diem**: Valor em DIEM para este detalhamento
  - **units**: Número de unidades para este detalhamento

### byModelDaily
Dados de gráfico diário para os 8 principais modelos. Cada entrada contém uma "date" (timestamp) mais os nomes dos modelos como chaves, com os valores de uso em DIEM.

### topModels
Array dos 8 principais nomes de modelos por uso, para legendas de gráficos.

### byKey
Detalhamento de uso por chave de API, ordenado por gasto total (maior primeiro).

- **apiKeyId**: O ID da chave de API, ou null se o uso veio do aplicativo web
- **description**: Descrição da chave de API ou "Web App"
- **totalUsd**: Total em USD gasto via esta chave
- **totalDiem**: Total em DIEM gasto via esta chave
- **totalUnits**: Total de unidades consumidas via esta chave

### byKeyDaily
Dados de gráfico diário para as 8 principais chaves de API. Cada entrada contém uma "date" (timestamp) mais as descrições das chaves como chaves, com os valores de uso em DIEM.

### topKeyNames
Array das 8 principais descrições de chave de API por uso, para legendas de gráficos.

## Exemplo de uso

```bash
# Obter analytics de uso dos últimos 7 dias (padrão)
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics" \
  -H "Authorization: Bearer YOUR_API_KEY"

# Obter analytics de uso dos últimos 30 dias
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics?lookback=30d" \
  -H "Authorization: Bearer YOUR_API_KEY"

# Obter analytics de uso de um intervalo de datas específico
curl -X GET "https://api.venice.ai/api/v1/billing/usage-analytics?startDate=2024-01-01&endDate=2024-01-31" \
  -H "Authorization: Bearer YOUR_API_KEY"
```
